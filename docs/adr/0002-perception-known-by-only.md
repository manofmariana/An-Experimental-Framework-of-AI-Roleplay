# 事件感知只走 known_by 标签

**决定**：事件能且只能携带 `known_by` tags；location 只是叙事记录字段，不参与感知过滤。

**为什么不保留 location 匹配通道**：双通道过滤（known_by ∧ location）会制造假阴性——跨场景对话事件的参与者地点不同，地点匹配会把"自己参与的对话"过滤掉。GM 标记时已基于推理考虑了各角色的可视情况（谁在场、谁参与对话、私密事件只标自己），机械过滤是冗余且有害的兜底。

**代价**：可见性质量完全依赖 GM 的标记纪律与提示词约束，程序不做机械兜底。