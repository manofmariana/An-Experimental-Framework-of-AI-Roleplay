# Agent-AIRP

多 Agent 架构的 AI 角色扮演系统：GM 导演 + 私域上下文角色 + 受约束正文渲染器，真相存于结构化事件日志。本词汇表随设计讨论逐步沉淀，当前聚焦 M2（多角色 + 连续时间 DES）。

## Language

**Event（事件）**:
真相层唯一记录单元，由 GM 裁决写入；`@CID` 占位，渲染时身份替换。**能且只能携带 `known_by` tags**——GM 标记时已推理过各角色可见性，感知过滤无地点成分、无其他限制 tag。事件存结构化 clock 值，自然语言时间头在注入/显示时按世界设定集时段映射表机械渲染。**GM 每轮产出的事件数 = 其计划划分的新组数**：在场组普通轮 = 1 个；对话组终结 = 对话事件 + 各新组（单人组/在场组）的事件。裁决包字段从简（`events[{text,tags,location?}]` / `narrativity` / `deltas` / `timer` / `location`），且**先写事件、后定 timer/location**——叙事推理自然约束变量分配。写作纪律（提示词协议层，不做程序强校验）：**摘要制**——不完整转述台词、对内容与情节做意义性总结；禁人称代词；保言行顺序、只记已发生；条目记录事实而非视角、禁止重合。
_Avoid_: 记忆条目、聊天记录；"摘要"（提示词层框架叫法，非领域术语）

**World Clock（世界时钟）**:
世界时间落盘唯一表示为 `world.time={y,m,d,h,min}`；逻辑 clock getter 以 y=365d、m=30d 派生绝对分钟，setClock 反写结构时间并记录可逆 `world.time` VarChange。自然语言头为“Y年M月D日·时段”。`time.json.start` 等于初始 world.time，仅保留开局锚与 periods 时段表。
_Avoid_: 回合数、轮次时间

**Timer（计时器）**:
**角色变量**——角色的到期时刻；GM 是唯一定立者，角色决策不含 span。**契约用相对偏移**：`timer: [{cid, span:{y?,m?,d?,h?,min?}}]`，程序 `due = clock + toMinutes(span)`、钳制 span ≥ 0（`{min:0}` = 立即）——GM 看到的是渲染后的自然语言时间，写绝对时刻只能瞎猜，相对偏移结构性避免负值/算错。跨轮对齐由注入解决：GM 提示词含派生快照"各角色 Timer 剩余时间"（占位符 provider 计算）。"组计时器统一" = GM 发往组的计时器被程序逐成员应用到各自的 Timer 上，故必然统一；组本身不持有计时器。可被**突发鉴定**打断。
_Avoid_: 倒计时、耗时（duration，已废除，见 docs/adr/0001）

**Character（角色）**:
主控角色（isPlayer=true，缺省 C0）与 NPC 的统称；所有角色状态完全同构：name/gender/age/personality/tags/reaction/location/timer/inScene/inTalk/initiative/level/isPlayer/relations/long_term_memory/vars。无 persona/voice、无 GM 伪角色 key。角色决策包必填非空 `action`/`intent`/`inner`，真实有值时追加非空 `dialogue`/`relations`；角色无 span，SpanSchema 仅供 GM timer。
_Avoid_: 仅指 NPC；通用 `changes` 字段

**isPlayer**:
角色变量，标记该角色当前由玩家操控；`await_player` 一律按 isPlayer 判定，不硬编码 CID——为切换扮演对象、多人操控角色预留。

**Level（等级）**:
角色必备变量；地点（Location）也带 level。两者差值驱动突发鉴定的良性/恶性与程度判定。

**Location（地点）**:
结构化变量 `{name, level}`——name 是 GM 裁决时赋予的自由文本（以世界设定集为参考、提示词管理；GM 在全员行动完后推理，会主动让剧情上在一起的角色拿到相同 name），level 是地点等级（突发鉴定用）。在场组的"同地"判定按 name。
_Avoid_: 纯自由文本地点（突发鉴定引入 level 前的旧形态）

**Initiative（先攻）**:
轮内行动排序的确定性投掷：d20 + reaction，总值同值即"同时"（同时行动者互不注入）；代码侧执行，不经 LLM。**结果写入角色变量，结构为 {先攻值， 组编号}**——组编号变化时重置（**变为 0 除外**：离场归单人组保留旧值，被召回原组时组编号对上即复用，不重投）；入组时已存值的组编号与当前组一致 → 复用，不一致/为空 → 该成员单独补投插入既有顺序（全组重投只发生在新组形成/SEQ 差规则判定重开时）。
_Avoid_: 速度、敏捷判定

**Reaction（反应属性）**:
角色变量，先攻修正值（d20 + reaction）；**可成长**（随角色经历变化），开场全员取同一值。

**Sync Group（同步组）**:
**派生状态，不是变量拥有者**——角色的 **Group 变量 = {组编号， 组位置}**（`组编号=0` = 单人组，恒有值；inScene/inTalk 双变量已合并，代码层不再区分组类型）。**成组 = timer 一致 + location 一致（自动）**；邀请可跨地点并入（见 Invitation）。**组位置总绑定先攻最高者的 CID**（= 其 location；领头者变更则重绑）；**角色入组时自身位置 ≠ 组位置 → 先攻 -1**（远程参与的劣后）。临时概念，随变量变化生灭。**单活跃组不变量**：任意时刻只有一个组在前台行动（DES 串行弹出）；前台组成员结构上不可被联系（联系标记目标只能是后台角色，**后台判定纯按 timer**，不看"刚才活没活跃过"）。**同一到期时刻弹出多组 → 逐组串行**：按组内最高先攻值降序，同值比该成员 CID 升序；每组跑至 GM 结算为止（**GM 契约：必输出非 0 timer**，成员推入未来后本组自然让位，下一组同刻开跑，不存在"跑多久换组"问题）。后台角色远程入组后，GM 依其行为（如挂断电话）把 timer 对齐回原组到期时刻，同地同时自动成组即自然回归原组（组 id 保稳延续原组身份）——回归不需要专门机制。**注入标注**：位置 ≠ 组位置的成员在注入中显式标注（远程参与），对齐计时器靠提示词约束 + 近期事件注入。组 id **指派并保稳**：成员集精确匹配 → 保留原 id；**纯减员 → 剩余成员继承原 id**（分裂则最大子集继承，并列取最小 CID）；**纯增员 → 也继承原 id**（已有成员先攻不变；新成员单独补投插入既有顺序，召回者复用已存先攻值）；**合组 = 人少并入人多**。全新组合 → 未用过的新 id。不能由 hash 派生——连续轮每圈重设 timer 都会换 id，连续场景判定即垮。
_Avoid_: 队伍、场景组；"组持有 location/计时器"（组位置绑定成员 CID，组不持有变量）；"在场组与对话组"、"在场同步组与不在场同步组"（两次旧二分，均已废除）

**Invitation（邀请）**:
拉人入组的唯一通道，由**联系标记**发起（途径字段——电话/视频/远程开门……——与对象列表）。**目标规则：至少含一个异组角色**（异组 = location 不同，或 location 相同但 timer>0 的角色）；**同组目标默认同意**——无组变动、无应答激活，单纯赋予频道变量；目标只能是**后台角色**（前台活跃组成员结构上不可被联系，后台判定纯按 timer）。**生效条件**：邀请标记生效时邀请者必在前台（其组 timer 到期）。**延迟生效**（防止时钟抢跑——立刻归零受邀者 timer 会抢在其旧组行动之前）：联系标记**触发 GM 立即结算**（GM 与受邀者都看不到邀请标记），邀请**与邀请者的 timer 同步生效**（邀请者组下一次弹出时落实），并算作**原周期的补完进程**：异组角色按**原有先攻按序**应答 →（若前台组未变）其他未完行动者补完 → **接受者进组并补投先攻** → 新组获得完整周期；若邀请者组曾进后台：回前台时行动顺序表重置（全员未行动），受邀者应答后直接进组、插入对应先攻位置并标**已行动**（无视先攻次序的优先行动）。**拒绝**（附理由）→ timer **自动还原**为被邀请前的值，不调用 GM，失去频道变量（全体持有者因此同地 → 频道自动清除），**拒绝回复不计入已行动**（随后台重置自动恢复行动权）。**接受**（确认标记 + 首轮回复）→ timer 归 0 直到 GM 重设，位置 ≠ 组位置 → 先攻 -1，**首轮回复计入被补完的旧周期已行动**。**感知过滤不在组层做**：M2-b 期间组内注入全透传；私密性归 P2 的顶层 TAG 过滤系统——酒馆闲聊与电话悄悄话同处一组，由 TAG 分流，**P2 过滤将基于 channel 变量实现**（2026-07 拍板：被拒邀请者的拒绝理由在 M2-b 全透传可见为既定行为，不作特例）。
_Avoid_: 对话组、远程组、不在场同步组（旧名）；AV 标签位（已撤回，由 P2 TAG 系统取代）

**Channel（频道）**:
角色变量（number|null），跨地点联系的"通话中"标识。**联系标记生效即分配**给邀请者与全部对象（同一频道 id）；**拒绝者失去**；**退组自动清除**。承担两个职责：①**防重入**——频道持有者不出现在任何联系人列表注入中（通话中/邀请建立中不可再被邀请，pending 与已建立统一处理），且**频道持有者/待答邀请者本身也看不到联系人列表**（占位符整体置空——角色必须从剧情与 relations 中对号 CID）；②**生命周期**——只要有任意两个持有者 location 不同，跨场景视为存在；**全部持有者 location 相同 → 频道变量全部清除**，仍非组位置的持有者直接离场（组归 0）等待下一次 GM 结算。
_Avoid_: 通话状态、房间号

**No-Adjudication Round（无判定轮）**:
轮的**默认形态**——一个行动周期内无人立 GM 请求标记，整个周期即无判定：角色照常输出台词与行为，工作集累积，GM 不激活。对话轮的升级：不再强制只输出对话，行为与台词有其一即可。**GM 激活两种触发**：①**标记触发**——周期内有人立 GM 请求**或联系**标记 → **立即激活**（不等周期末——开门类判定不能等，否则其他角色会基于未裁决状态继续行动；联系标记触发 GM 是为延迟生效的邀请结算 timer，见 Invitation；**同先攻批次必须全员行动完才激活**，批内同时性不被裁决撕裂）；②**硬保险**——距上次 GM 激活满 N 个行动周期（可编辑数值）→ 周期末激活。**立即激活（中途 GM）的 timer 契约 = 精确覆盖全体同步组成员 + 刚从同步组离开的成员**（程序强校验：缺少/越界/重复即拒绝重试）。覆盖未行动成员恰恰是周期补完的前提——**周期序列与 timer 无关，是组自身的状态**，acted 变化有且仅有三种：①行动后 → 已行动；②全组已行动 → 全员重置未行动、X+1；③组结算后进后台 → 全员重置未行动、X 归 0。未行动成员保持未行动，span 一致则组状态不变、周期不乱；恰恰是不设 timer 才会让他们与已行动者 span 不一致、被编入别组、打乱周期。组存续规则：timer 同步 → 组编号存续，行动顺序由 {先攻值，组编号} 派生（同值批内按 CID 升序）。软保险 = 提示词层的退出期望管理。
_Avoid_: 对话轮（旧名）；普通轮

**Marker（标记）**:
角色/玩家输出中的结构化指令位，**DecisionPackage 的可选 `markers` 数组**（每条 `{type, ...params}`：`gm_request` / `leave`（二者互斥）/ `recall{target}` / `contact{channel, targets[]}` / `confirm`），程序只读结构化字段、不做行内文本解析；提示词按结构化格式教学。标记**不进工作集、不进任何注入**——解析后只有即时程序作用（调度/组别/timer 变更），即抛；离开/召回等经过的可见性由未来的注入渲染机制承担（推迟到提示词组装设计阶段，M2-b 落地时无渲染）。五种：
- **GM 请求标记**：请求 GM 裁决；**与离开标记二选一**。同先攻批全员行动完后 GM 立即激活（见 No-Adjudication Round）。
- **离开标记**：离场**不触发 GM**——程序当场将其组编号归 0、timer 置超大值；结算（离场事件、新 timer）推迟到下一次 GM 激活（标记触发或硬保险）。**对所有角色（含玩家）统一程序化**（玩家例外已废除）；离场者的过去言行继续注入原组（直到 GM 清算）；后续角色经 `{{departure_notices}}` 占位符获知离场与召回方式。
- **召回标记**：把已离开且未结算者拉回——timer 重新归零（即组原到期时刻）、按进组规则归组，**先攻不变、发言顺序保持**（复用已存 {先攻值，组编号}，组编号对上即有效）。**跨周期有效**（离开→GM 结算前均为"未结算"）；召回提醒与"已召回"通知的注入渲染见条目首部（推迟实现）。
- **联系标记**：发起跨地点邀请（途径字段 + 对象列表），机制见 Invitation。
- **确认标记**：被邀请者接受邀请（+ 首轮回复）；拒绝则输出理由、不立此标记，timer 自动还原。
_Avoid_: 指令、命令、工具调用

**Action Cycle（行动周期）**:
连续场景内**所有成员各行动一次**的计数单位；硬保险"GM 强制间隔"以行动周期计（N 个行动周期后强制激活 GM）。与"轮"（单次激活 seq）、连续轮（旧含义）区分。**周期补完**：前台组不变 ⇒ 已行动/未行动状态不变，未行动者继续行动，直到原周期成员全部行动完，再进入下一周期或被并入新组——保证一个组只要不进入后台就一定能进行完整的周期活动（人少并入人多的组变动发生在补完之后；中途 GM 后未并组、组仍前台 → 未行动者继续，同属补完）。进度管理见 Action Order Table。
_Avoid_: 圈、回合

**Action Order Table（行动顺序表）**:
顺序 = **派生**（只有前台组用得到）← 角色 initiative 变量（{先攻值，组编号}）现排；**已行动位 = 角色变量**（快照落盘，var_changes 自动落账、回溯可逆——程序在角色行动后置位，周期完成/后台重置时清零；不做 archive 回扫——恢复时无需判断步骤在不在本周期内）。成员增减兼容：接受者应答即程序置其已行动位（后台回归时 = 无视先攻次序的优先行动，插入对应先攻位置）；离开者从当前成员集移除，完成判定只看当前成员。**周期计数 X = 世界变量**（var_changes 通道）：周期完成 +1，任何 GM 激活后清零；X 达阈值 N → 周期末 GM。中途 GM 的补完自动成立（不动已行动位，无特例）。**取代 SEQ 差规则与组级存表方案**——GM/正文步不参与调度语义。
_Avoid_: 轮游标、pipeline 快照、组级先攻排序存储、archive 回扫定行动状态

**Continuous Scene（连续场景）**:
同一个同步组（同一组编号存续期间）的全部轮次。正文归档记录该轮的 `participants` 与行动时的 `scenes[CID]`；角色正文滑窗只取包含自身且归档场景 id 与当前相同的最近 N 段，GM 只取与本轮行动者连续的正文，正文 agent 保持全量近期正文。
_Avoid_: 旧定义"至少一个角色无计时器 pending"；按全局最近正文注入

**Surge Check（突发鉴定）**:
每次 GM 裁决结束后执行的三步机械鉴定（引信制的细化，DESIGN §5.5），可打断 Timer。记 Δt = 当前时刻到最近 Timer 的时间差，g = 差值函数（地点 level − 角色 level）∈ (-50,50)：① 突发点数 = (d20-1) + f(Δt)，f ∈ (0,80] 随 Δt 单调递增 → 触发概率 ∈ (0,99]%；② 良性概率 = (g + 50)%；③ 程度 = (d20-1) + |g| + 30 ∈ [30,99)（+30 = 避免无意义事件的修正值）。**所有 [a,b] 区间值均为百分比**：①②直接按百分比概率检定（d100 ≤ 点数即成立）；③程度为烈度百分比，供内容生成参考。专门的鉴定函数在程序侧实现；解释性提示词未来做，先留记录。
_Avoid_: 随机遭遇（那是桌游术语，此处是程序化三步鉴定）

**Working Set（工作集）**:
当前轮未裁决的角色言行暂存区；GM 裁决时才转写为 Event，此前不落事件库。
_Avoid_: 暂存消息、上下文

**Prose（正文）**:
小说视图渲染器的产出；GM 事件经字段提取、提示词组装后注入（"何时何地，哪些人做了什么"），一次 GM 包（N 事件）渲染一次（narrativity 包级控制，skip 则无正文）。注入中 GM 事件**之前**还有：本轮各角色的台词+内心（inner 含意图——intent 字段已并入 inner，随内心一并作为正文情绪参考）。渲染范围 = 全量事件（不按玩家视角过滤）；限定视角（第一/第二人称）未来经 known_by tag 过滤即可扩展。存储原文带**指称占位符**（见下条）。
_Avoid_: 输出、回复

**Reference Placeholder（指称占位符）**:
正文存储原文中指称演员表角色的占位语法 `[[称呼|@CID]]`——称呼 = 当时为人所知的名字/外号/特征称谓，@CID 绑身份；被引号包裹的直接引语豁免，照写原文。渲染看读者：玩家显示 → 称呼；角色注入 → 按读者 relations（name → impression → 保留 @CID）；GM 注入 → 称呼（@CID）；正文 agent 的上一轮正文注入保持原文（格式学习通道）。与 Event 的 @CID + 身份替换同一"存储用 CID、渲染看读者"体系（docs/adr/0003）。
_Avoid_: 正文裸名直存、正文裸 @CID 直存

## Relationships

- **Sync Group** 是派生状态：程序按 **Character** 变量机械划分（timer+location 一致自动成组；**Invitation** 可跨地点并入）并指派保稳 id；**Group = {组编号， 组位置}**，组位置绑定先攻最高者 CID，组不持有任何变量
- 双变量叠加态已废除：inScene/inTalk 合并为单 **Group** 变量，`组编号=0` = 单人组；**调度判定统一为 Timer 模式**（扫描最近到期者），不再有对话模式特例
- **Timer** 与 **location**（{name, level}）是角色变量；GM 发往组的计时器由程序逐成员应用（故组内统一）；独立改某角色的 location/timer 即离组
- **World Clock** 只在调度器弹出最近 Timer 时跳转；全体角色的 Timer 先后顺序 = 轮次顺序（"下一步该谁"）
- 一个 **Action Cycle** 属于一个 **Sync Group**；言行暂存 **Working Set**，GM 激活后转写为 **Event**
- **Event** 的可见性 = `known_by` tags 唯一通道（GM 推理后标记）；location 字段只作叙事记录，不参与感知过滤
- **Prose** 与 **Event** 同一"存储用 CID、渲染看读者"体系：事件身份替换走 relations（renderForReader），正文指称占位符按读者分视图（玩家=称呼 / 角色=relations 过滤 / GM=称呼（@CID））
- **时间轴与轮状态全部派生，无独立存储**：调度 = 扫描角色 Timer 取最小；组 ← timer+location/邀请；行动顺序 ← Initiative 变量（{先攻值， 组编号}）；游标与 phase ← archive 的 seq 序列推断（archive 不存 pipeline 快照）
- **Surge Check** 在每次 GM 裁决结束后执行，以 **Location.level** 与 **Character.level** 的差值为核心参数，可打断 **Timer**

## Flagged ambiguities

- "在场组"原定义为感知隔离单元（DESIGN-P1 §1.1），M2 重定义为同步组的一种类型——感知隔离职责上收到同步组。
- "耗时（duration）"与"计时器"曾并存；已统一为计时器——耗时只能推动时间、没有激活功能，多组交替时会导致时间错误跳跃（见 docs/adr/0001）。
- "轮"被用于三种含义：总轮次 seq（一次 API 调用）、连续轮（同步组内一圈行动）、对话轮——须逐处指明。
- DESIGN-P1 §5.1.3"退出时选择是否输出行为、意图与内心"已修订为**退出轮必须输出行为意图**——"纯聊天终结无 GM"的情形因此不存在。
- DESIGN-P1 §1.1"GM 控制在场组的划分与变动（可改组）"已修订：分组由程序按角色变量机械判定，GM 只设 location/timer 变量。
- DESIGN-P1 §5.2"对话消耗的叙事时间由 GM 在终结裁决中以 duration 一并结算"已修订：对话机制上瞬间发生，GM 不结算对话耗时，只在最后一人退出时设定各角色下一步 Timer。
- "玩家固定 C0"修正为：M2 缺省主控是 C0，但玩家身份判定一律走 isPlayer 变量（未来可切换扮演对象/多人操控）。
- simulator.ts 的 `tags ∧ location` 双重感知过滤已废除：事件能且只有 `known_by` tags，GM 标记时已考虑各角色可视情况（见 docs/adr/0002）。
- 地点枚举表方案被否：location.name 是 GM 自由文本（世界设定集为参考 + 提示词管理），不建枚举表；突发鉴定引入后 location 结构化为 {name, level}。
- M1"一轮只产出一个事件"（DESIGN-P1 §10.1.2）已修订：**事件数跟随 GM 的新组划分**——对话组终结时 GM 一次产出对话事件 + 各新组事件；组划分表面自动、实际由 GM 决定（划分判据 timer/location 都是 GM 设的），故裁决包先写事件、后定 timer/location。
- GM 裁决包字段从简：`timer_settings→timer`、`location_updates→location`、`state_deltas→deltas`（节约 token）。
- DESIGN-P1 §10.1.2"正文输入 = 角色全部响应（含行动意图）"已修订：正文只拿各角色的**台词+内心** + GM 事件包。（二次修订：intent 字段并入 inner，内心含意图。）
- "archive 每步存 pipeline 快照"方案被否：组/行动顺序/游标/phase 全部可从角色变量 + archive seq 序列派生，只需角色变量多一个最近先攻结果。
- "单人组 inScene 置空 vs 标记"已定为**标记**（inScene=0 = 单人组，恒有值）；对话叠加不做 inScene 置空-恢复，而是 inScene 与 inTalk 同时有值（合法组合仅 4 种，见 DESIGN-P1 §10.3 状态表）。
- "既在对话组又在场组"不是冲突：叠加态（inScene 与 inTalk 同时有值）是设计表达；被杜绝的是"在场归属不确定"，由 inScene 单值化结构性保证。**（已被 M2-b 二次修订废除：inScene/inTalk 合并为单 Group 变量，叠加态不复存在）**
- GM 事件的 @CID/known_by 纪律靠提示词协议约束，不做程序强校验（叙事自由度优先：参与者不知道的事不标他们）；提示词模板示例禁用真实 CID（抽象占位 `<CID_甲>`），防示例感染。
- "摘要"是 gm.prompt.json 对事件的提示词层叫法，目的是约束 GM 输出（摘要制写作：不完整转述台词、禁人称代词、保序、只记已发生）；领域术语与讨论基础仍是"事件"。此前"台词逐字引用原文"的转写纪律已废除——正文台词原文保真走角色言行注入通道（renderSpeech），不依赖事件转写。
- M2-b 重分类（2026-07）：同步组二分从"在场组/对话组"改为**在场同步组/不在场同步组**；对话轮升级为**无判定轮**（默认无判定，GM 标记触发立即激活/硬保险周期末激活）；离开行为从"GM 裁决"改为**离开标记程序化归 0 + 超大 timer、下次 GM 统一结算**；硬保险阈值 N 存 config.json（设置页可编辑）；**玩家输入计入行动周期计数**（isPlayer 不特判），且主控输入格式与 NPC 输出统一（三块：台词/行动/内心（含意图）+ 标记按钮）列入 M2-b 必做；DecisionPackage 契约简化：**inner 必填（intent 并入）、action/dialogue 至少其一**（存档 v5）。
- M2-b 二次修订（2026-07）：**inScene/inTalk 合并为单 Group 变量 {组编号， 组位置（绑定先攻最高者 CID）}**，在场/不在场组类型废除，叠加态与 4 合法组合表作废；**入组位置 ≠ 组位置 → 先攻 -1**；邀请激活受邀者一次（拒绝 → timer 自动还原、不调 GM；接受 → timer 归 0 直到 GM 重设）；**AV 标签位撤回**——感知过滤不在组层做，私密性归 P2 顶层 TAG 过滤系统（M2-b 期间组内注入全透传）；先攻变量结构化为 {先攻值， 组编号}（组编号变化即重置，归 0 除外）。
