# Ofair（Open Framework of AI Roleplay）

多 AI 角色扮演系统：一个 GM 导演 + 拥有私域上下文的角色 activation + 受约束的正文渲染器，真相存于结构化事件日志而非聊天文本。

本地运行的私有应用，无打包/发布流程。启动：`Ofair-WebUI.bat`（WebUI）或 `npm run dev`（CLI）。开发对接见 `AGENTS.md`。
