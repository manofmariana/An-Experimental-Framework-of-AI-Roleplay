# Agent-AIRP 设计文档

> 状态：P0 已实现 + P1-M1 现状对齐 + P1-M2 设计冻结（2026-07）——已落地机制以代码现状为准，M2 条款为冻结设计（DESIGN-P1 §10.3）。
> 定位：用多 Agent 架构取代 SillyTavern 式单上下文结构，实现更好的上下文管理、缓存命中率、多角色认知隔离与即时功能拓展。
> P1 细化（多角色与事件记录体系）与实施决断见 `DESIGN-P1.md`；领域词汇表见 `CONTEXT.md`；架构决策记录见 `docs/adr/`。

---

## 1. 设计哲学

### 1.1 SillyTavern 式结构的病灶

ST 的本质是**单上下文、单人格、全知视角的文本拼接器**：

1. 单体 prompt 组装：角色卡 + 世界书 + 全部历史每轮拼成一个大字符串，一个模型精分所有角色。
2. 缓存杀手：世界书按深度插入、历史尾部增长，前缀缓存每轮被击穿。
3. 无认知隔离：模型天然全知，秘密/谎言/悬疑/戏剧反讽在结构层面不支持。
4. 无状态真相源：聊天文本即真相，改一条历史全靠手动，无一致性校验。
5. 扩展是前端 JS 补丁：前后端强耦合，新能力开发成本高。

### 1.2 本架构的四条哲学

1. **作者退后，角色自治**（参考思路.txt）：角色是独立的人，不知道自己在作品里。主要角色的台词、决策、记忆由角色自己的 agent 全权负责，任何其他 agent（包括 GM）不得替角色行动、感受、思考。
2. **真相层与视图分离**：结构化事件日志 + 变量库 + 知识账本是唯一真相；正文、角色感知、前端画面都只是真相的渲染视图。视图永远不许反向污染真相。
3. **需要判断的才配当 agent，纯工序做成确定性服务**：变量落库、感知投递、计时器、向量嵌入、骰子——零 LLM。LLM 调用只花在判断和创作上。
4. **通用优先于特供**（借鉴 AIRP-MCP-Server）：核心保持后端无关，模型特定的优化（如 Anthropic `cache_control`）下沉到边缘层。

---

## 2. 总体架构

```
                        玩家输入（主控意图）
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
  角色Subagent ×N      （次要NPC由GM代演）      主控=玩家本人
  自主决策：台词/行动意图/     │
  打断响应/人际关系维护        │
        │  决策包（结构化）    │
        └──────────┬──────────┘
                   ▼
        ┌──────────────────────┐      T0 机械层（零LLM）：
        │  T3 GM-Subagent      │      调度器服务：事件队列、
        │  重裁决/NPC/世界反应  │      计时器、感知投递、唤醒、
        │  @ID 转写/事件可见性  │      变量落库、向量嵌入
        └──────────┬───────────┘
                   │ 裁决包（events + deltas +
                   │  timer/location + narrativity）
                   ├──────────────► 事件日志（真相层，append-only）
                   │                        │
                   ▼                        ▼
        ┌──────────────────┐     ┌─────────────────┐
        │ 正文Subagent      │     │ 变量应用器（确定性）│
        │ 纯渲染器，无工具   │     │                  │
        │ 无权改写角色台词   │     │                  │
        └────────┬─────────┘     └─────────────────┘
                 │ 结构化事件流（speech/action/state/ui/media）
                 ▼
        瘦前端（纯渲染器；CLI + WebUI 双实现）

  工具SubAgent（按需，离关键路径）：
  世界书条目创建/合并、文风/预设修改、前端配置修改
```

注：这里的角色/GM/正文"agent"是效果与理解上的逻辑概念——底层是**单轮独立调用**，每次激活 = 从数据层组装提示词的全新请求，无对话历史、无工具调用能力。唯一能称为真正 Agent 的是**工具 SubAgent**（未来的工作，具备读写与执行能力）。

---

## 3. 裁决分级制（T0–T3）

调度和裁决严格分离。**GM 不在角色激活链上**——唤醒是机械工序，零开销。

| 层级 | 执行者 | 职责 | 开销 |
|---|---|---|---|
| T0 机械层 | 调度器/服务（无 LLM） | 计时器触发、事件按 known_by 过滤投递、唤醒角色、变量落库、向量嵌入、骰子数学 | 零 token |
| T1 自主层 | 角色 Subagent | 打断响应（继续/放弃/改行动）、台词、自评"是否需要裁决"、人际关系库自维护 | 小输入小输出 |
| T2 轻裁决 | 规则工具（确定性） | 骰子+属性 vs DC 等规则化判定；结果连同意图上报 GM 做后果描述 | 零/极小 |
| T3 重裁决 | GM-Subagent | 冲突裁决、开放性结果、世界反应、次要 NPC 决策、叙事权重 | **主输入开销**，攒裁决窗口批量处理 |

**GM 调用调节旋钮**：裁决窗口大小（延迟 vs 成本）。因果独立的多个意图攒一批一次裁决。

---

## 4. 各 Agent 职责详述

### 4.1 角色 Subagent（×N，主要角色）

- **独立私域上下文**：只含该角色能感知的信息。认知隔离由结构保证，不靠"请假装不知道"。
- **单轮调用**：每次激活 = 从数据层（事件集/角色变量/工作集/正文滑窗）组装提示词的独立请求，连续性靠注入而非对话历史。
- **唤醒制**（非轮次制，M2 落地）：角色携带 `timer` 变量，到期即被调度器唤醒（行动完成 / 长期目标到点）；组（在场/对话）是程序从角色变量派生的状态。沉睡角色零开销。
- **决策包输出格式**：`action` / `intent` / `inner` 三个非空必填字段；真实有台词时追加非空 `dialogue`，真实关系变化时追加非空 `relations`。角色不得输出 span；`SpanSchema` 仅属于 GM timer 契约。
- **记忆 = 视角化渲染**：角色的记忆是可见事件记录经身份替换的第一人称渲染（自己→我/真名/印象称谓）；人际关系（名字/印象）由角色全权自维护，GM 不参与（DESIGN-P1 §1/§3）。
- **isPlayer 变量**：标记该角色当前由玩家操控（M2 缺省 C0）；`await_player` 按 isPlayer 判定而非硬编码 CID——为切换扮演对象、多人操控预留。

### 4.2 GM-Subagent（T3）

- 唯一全知者，但**克制**：不替主要角色行动/感受/思考（红线）。引导剧情靠环境旁白吸引注意力，靠 NPC 和世界反应推进。
- 职责：重裁决、次要 NPC 代演、世界反应、为每个行动者订立 **timer**（相对偏移 span，连续时间）、设置 location（自由文本，组划分由此派生）、以 `known_by` tags 逐事件标记可见性（唯一通道，docs/adr/0002）、**narrativity**（包级，进入正文的权重）、新条目提案。lore 激活为触发制（DESIGN-P1 §10.1-10）。
- **裁决包**是唯一输出形式（M2 契约，DESIGN-P1 §10.3——先写事件、后定 timer/location）：

```json
{
  "events": [{"text": "@C1002 的剑刺中 @C1003 左肩，@C1003 后退撞翻烛台",
              "tags": ["known_by:C1002", "known_by:C1003"],
              "location": "灯塔顶层"}],
  "narrativity": "full",
  "deltas": [{"path": "vars.chars.C1003.hp", "op": "-=", "value": 17}],
  "timer": [{"cid": "C1002", "span": {"min": 30}}],
  "location": [{"cid": "C1003", "location": "灯塔顶层"}]
}
```

### 4.3 正文 Subagent（纯渲染器）

- **无工具、无检索权**。世界知识 = 静态世界基调卡 + 世界设定 + 本轮触发 lore（在场角色固定标签激活条目的并集，去重按 ID 排序；DESIGN-P1 §10.1-10）。
- 输入：协议（含基调卡）→ 世界设定 → 近期事件（演员表渲染）→ 演员表（CID 与名字）→ 触发 lore → 上一轮正文（仅此一轮，行文衔接）→ 各角色台词+内心（inner 含意图，作情绪参考；标记不注入）→ 本轮 GM 事件包。正文不注入 GM 公共长期记忆；台词原文所有权在角色 agent，**无权改写**。
- 渲染范围 = GM 包全部事件（一次 GM 包至多一次正文调用，narrativity 包级；多组交替 = 蒙太奇叙事，不做玩家视角过滤——限定视角未来经 known_by 过滤扩展）。
- **指称占位符**：正文存带 `[[称呼|@CID]]` 的原文（直接引语豁免），显示渲称呼、角色注入按 relations 渲染、GM 注入渲 称呼（@CID）——与事件的 @CID 存储 + 身份替换同一"存储用 CID、渲染看读者"体系（docs/adr/0003）。
- **防出戏三层**：① 世界基调卡（~300-500 token 元设定，静态进前缀，挡 gross 级出戏）；② 触发 lore 条目（挡场景级出戏；秘密条目访问控制 = 标签，不持有对应标签即结构上不进正文）；③ 异步校验 pass 兜底（未来）。
- **防添油加醋闸门**：① 硬约束块（必须且只能覆盖本轮 GM 事件）；② 标签隔离（秘密 lore 只随知情角色标签触发）；③ 信道分离——台词与关键动作所有权在角色 agent，正文只写结缔组织（最强的一道，结构性保证）；④ 异步校验（未来）。
- 输出结构化事件流：`speech / action / state / ui / media`，前端纯渲染（P3）。

### 4.4 变量体系

- **变量应用器 = 确定性代码**：GM 输出的 deltas 直接落库。零 LLM、零误差、可审计、可回滚——全部变量变更带 `{path, before, after}` 记录，回溯 = 倒序反向执行（DESIGN-P1 §10.2）。
- 变量写入当前由 GM 裁决驱动；**未来角色也将有部分变量写入能力**（如自行登记消耗品、整理随身物品），写入通道同样走确定性应用器。

### 4.5 工具 SubAgent（按需，离关键路径；未来）

- 世界书条目创建/合并（见 §7 治理）、文风/预设修改、前端配置修改。提示词编辑职能已由 Web「提示词」页签承载。
- **修改生效纪律**：提示词模板（`data/prompts/`）每轮激活前热加载——Web 保存后下一轮即生效（DESIGN-P1 §10.1-11）；世界书/角色/配置等其他资源维持"新会话生效"。模板类修改 = 主动击穿缓存前缀，编辑者知情接受。

---

## 5. 时间与调度：连续时间轴 DES

### 5.1 模型

- **连续时间**（硬性要求，不做时间量子化）。全局世界时钟 T；最小单元 1 分钟，记录格式 {y/m/d/h/m}；自然语言时间头经世界设定集时段映射表（`time.json`）机械渲染，LLM 不自由生成。
- **timer 是角色变量**（M2 定稿，DESIGN-P1 §10.3）：只能由 GM 以相对偏移 span 订立（到期 = clock + span，钳制 ≥ 0）；角色决策不含 span。**耗时（duration）概念废除**——计时器是唯一时间跨度设置单元（docs/adr/0001）。
- **调度 = 投影**：扫描全体角色 timer 取最近到期者激活，时钟跳转到该到期时刻——时间轴无独立存储；组同样是程序从角色变量派生的状态（location+timer 自动成组，邀请跨地点并入；Group = {组编号， 组位置}，inScene/inTalk 双变量已于 M2-b 合并）。（M2 落地；M1 主循环为固定顺序编排 player→character→GM→prose，DESIGN-P1 §10.1-3。）

### 5.1.1 调度架构：语义统一 + 实现分层（已定）

"调度"拆成两个领域，接口只有 `Activation` 与 `Event` 两个类型：

- **模拟调度器（语义层，必须统一）= 纯逻辑**：M2 重写为**派生纯函数集**——`next_due`（取最近 timer）/ `derive_groups`（Group 派生，保稳 id）/ `visible_events`（known_by 过滤）+ 先攻排序。**无 IO、无 LLM、纯函数式**——输入角色变量与真相层状态，输出激活指令（唤醒谁、带什么感知包、什么顺序）。组与轮状态全部派生，避免状态双写。可纯单测：脚本化序列喂入，断言激活输出，全程无 LLM；retcon = 截断日志 + 变量倒序反向执行的纯数据操作。
- **运行时编排器（计算层，可简陋可替换）**：拿到 Activation 后决定 LLM 调用怎么执行（P0 顺序 for 循环；P1 并行+攒批），不知道任何时间语义；模拟层也不知道 LLM 存在。
- 反模式警示：不用 wall-clock（setTimeout）表达世界内时间；不用事件队列管 HTTP 并发。

### 5.2 防时间脱同步三机制

> 注：早期版本有"节拍（beat）= GM 宣告的全局同步点"概念，已废弃——GM 宣告即调度开销，预设即剧本化，均不可接受。其职能由以下机制与事件本身接管。

1. **交互强制对齐**：A 对 B 说话的那一刻，B 的时钟快进到交互时刻。时间偏斜只允许存在于互不感知的区间——而互不感知时偏斜本不可观测，无害。
2. **事件即同步点**：广域可感知事件（爆炸、闯入者）落地时，感知半径内所有角色被同时唤醒——同步由事件天然完成，无需任何宣告。
3. **感知过滤 + 相对时间**：唤醒时从事件日志取 `time ≤ 唤醒时刻 ∧ tags 含 known_by:自己` 的事件——**known_by 是感知唯一通道**（事件能且只有 known_by tags，GM 标记时已推理各角色可见性，docs/adr/0002）；绝对时间只在真相层，提示词中的时间/地点经占位符注入——M1 注入结构化世界时间，自然语言时间头（"第3日·入夜后"）随 M2 落地（time.json 时段映射表，DESIGN-P1 §10.3）。

### 5.3 GM 后台化的认知同步

> **真相层是唯一同步信道；任何 agent 不读 GM 的进行中状态。**

- GM 是**事件日志**的唯一写入者（单写入者原则，无竞态）。变量写入当前由 GM 裁决驱动，未来角色也有部分变量写入能力（§4.4）。角色只提交意图。
- 裁决完成 = 事件 commit 进日志。正文只读已 commit 的事件窗口；角色感知只读已 commit 的日志尾部。
- 冲突（同时抓同一物品）在事件触发时刻由 GM 裁决，后到者收到"打断"感知并重决策（T1）。

### 5.4 正文与时序解绑

正文绑定**叙事窗口**而非轮次。窗口关闭触发条件（满足其一，纯机械）：素材量阈值 / 玩家需要反馈 / 重大事件落地。narrativity（包级）决定每个 GM 包全文渲染还是一笔带过。收益：正文调用频率降低、文风前缀缓存利用率提高、输出成本摊薄。

### 5.5 突发事件：引信制（双骰）

原则：**事件落地的那一刻，其内容必须已 commit 进真相层，激活是纯机械工序——GM 不在激活链上（含突发事件）**。

- 骰子在 T−lead 决定触发并抽出引信时长 lead（连续时间）。流程：① 骰中 → "待填内容"事件入队，落地时间 T+lead；② GM 在下一个裁决窗口（背景攒批）生成内容——具体发生什么、涉及哪个 NPC、NPC 开场行为与意图——commit 进真相层；③ 到期 T0 读已 commit 内容机械激活 NPC，NPC 后续决策走常规 T3 窗口；④ 引信到期内容未就绪 → 顺延短引信或静默流产，**绝不阻塞世界**。
- **前提条件复查**：内容生成距落地有时间差，落地前 T0 机械复查前提（地点/在场角色/世界状态约束），失配则事件流产/改期并在下个窗口告知 GM。
- **张力预算**：骰子池参数（触发率、引信分布）由 GM 在场景级一次性设定，之后机械执行。GM 的导演意志体现在参数上，而非每次触发亲自到场（桌游随机遭遇表的智慧）。环境质感细节（变天、喧哗）由正文 agent 在结缔组织里自由发挥，不单设事件类型。
- **M2 细化为三步突发鉴定**（DESIGN-P1 §10.3）：每次 GM 裁决结束后机械执行——① 是否突发（d20-1 + 时间差函数，时间越远越高）；② 良性/恶性（地点 level − 角色 level 差值函数 +50）；③ 程度（d20-1 + 差值绝对值 +30）。所有区间值均为百分比，直接按概率检定。location 随之结构化为 `{name, level}`，level 为角色必备变量；突发可打断 timer，长期行动不再必然跑满。

### 5.6 行动轮：先攻排序模型（还原桌游）

一个机制覆盖所有调度情况：**先攻（initiative）= d20 + reaction**（reaction 为可成长角色变量，开场全员同值），由 T0/T2 机械掷出并排序，GM 不在激活链上；**总值同值即"同时"**——同时行动者互不注入。投掷结果写入角色变量（{先攻值， 组编号}），行动周期的行动顺序由此派生还原。

- **域内有序，域外并行**：先攻排序只在互相可感知的角色（同一感知域/遭遇，通常 2-4 人）内有意义——遭遇/刺激发生时，域内角色按先攻序依次激活，**前序角色的已定行动 commit 后进后序角色的感知包**，行动衔接、掩护、配合、补刀自然涌现。不同地点、互不感知的角色之间无顺序可谈，天然并行，串行的墙钟代价被限制在遭遇内部。
- **同值 = 同步激活**：先攻同值的角色同时激活、互不知道对方行为；意图若冲突，进 T3 窗口由 GM 发起**对抗检定**（比大小）并叙述结果——"两人同时扑向一台电脑"的戏剧张力保留为同值特例，而非常态。
- **因果链免费**：A 的行动 commit 后本身就是事件，B 被唤醒时感知包自然带着它——对话回合（你说→我答）无需任何专门机制。
- **打断判定复用先攻**：事件落地时，先攻高者的进行中行动来得及收手，低者被迫硬吃（同时解决原开放问题"打断粒度"）。
- **次要 NPC 特例**：由 GM 代演，其先攻排序与相互冲突在 GM 一次裁决调用内部解决，零额外开销。

### 5.7 玩家时间线与快进模式

- **主控是时间轴上的一等实体**：与其他角色一样有 `timer`（isPlayer 变量标记操控权，await_player 按 isPlayer 判定），行动同样由 GM 订立 timer。绝大多数玩家行动是瞬时的，世界体验上"等玩家"。
- **长期指令**（如"闭关两年"）：主控进入"进行中长期行动"，世界按时间轴照走——中间的事件与 NPC 行动轮按序推进。玩家唤醒条件：行动完成 / 重大可感知事件打断 / 玩家主动打断。
- **快进模式（timeskip）**：主控待机期间，GM 与正文**切换到第二套提示词**——NPC 行为笼统长期化、裁决大步长、叙事编年化（montage 而非场景级正文），以压缩墙钟。发生重大事件则自动暂停快进、回到常速模式细写。
- 快进/常速是**同一 agent 的模式切换**（换模式卡），不是新 agent；缓存按模式各自命中，代价仅是切换后首次调用的前缀重建。

---

## 6. 真相层与记忆系统

### 6.1 真相层（第三人称，GM 侧）

- **事件日志**：append-only，按世界时间有序，唯一真相。存档 v2：`runs/{id}/events.json`，事件带 seq 锚 + 统一 tags（DESIGN-P1 §10.2）。
- **变量库**：HP/位置/物品/关系值等，由确定性应用器按 deltas 落库（world.json + characters.json，全部变更带 before/after 记录，§10.2）。
- **知识账本**：落地形态为统一标签体系——事件与 lore 的可见性共用一套 tags，`known_by:{CID}` 即标签，驱动感知过滤（DESIGN-P1 §2）。

### 6.2 认知层（第一人称，角色侧）

- 真相是第三人称的，**呈现是第一人称的**：编译给角色的内容经身份替换转写为角色本人的认知（"我记得那个新来的，说话有点紧张"）。
- **记忆 = 可见事件记录的视角化渲染** + relations 人际关系自维护（§4.1）。
- 三层记忆（P2 候选）：**BOOT**（核心人设，全量注入）/ **场景记忆**（近期经历自动带回）/ **琐碎记忆**（不自动浮现，注入时按相关性检索）。
- **向量化流水线**（P2 候选）：记忆条目 → 嵌入服务异步批量向量化 → 注入组装时按相关性检索（系统侧工序，非角色能力）。
- **记忆整理归角色自己**（Audit 模式，场景外跑，P2 候选）：角色删过时、并重复、调触发——"角色最清楚什么对自己重要"。

### 6.3 场景外模式（角色在舞台之外也活着）

Chat（与作者闲聊）/ Audit（记忆整理）/ Diary（第一人称叙事创作）/ IM 接入（如 QQ，共享同一套人设与记忆）。场景结束后 GM 可审查逻辑、归档、润色互动记录为叙事文本（"作家"流程）。

### 6.4 Retcon

结构化真相层使 retcon 可行，落地形态为 **seq 回溯**：events 按 seq 截断、变量按 var_changes 倒序反向执行（before 写回）、lore 按 changelog 反向回滚，逐字节回到目标步刚完成的位置（DESIGN-P1 §10.2）。取代 ST 的文本编辑（"改第 50 楼崩后面 200 楼"）。

---

## 7. Lorebook 治理（活的世界圣经）

- **触发制**：GM 不为正文激活/禁知条目；正文 lore = 在场角色固定标签激活条目的并集（去重、按 ID 排序）；秘密条目访问控制 = 标签（如 `灯塔：秘密` 只随持该标签的角色进正文）（DESIGN-P1 §10.1-10）。档内 lore 为副本——新会话把世界 lorebook 拷入存档，运行期增删改只动副本，不污染原始设定集（§10.2）。
- **新条目创建**（GM 提案 → 工具 SubAgent 执行，未来）：
  - 先查重后创建：关键词 + embedding 相似度检查，超阈值直接复用已有条目；
  - 类型白名单：地点/物品/次要 NPC 可自由创建；主线实体或修改已有条目挂起等用户确认；
  - 溯源标记：`provenance: gm_generated | user_authored`；
  - 节流：每场景新建上限；场景外定期 consolidation（合并相似、降权从未激活的）。
- **检索策略分域**：lorebook 要准——关键词预筛 + 小模型按 ID 拣选（确定性、缓存友好）；角色记忆检索可以糊——embedding 向量检索（模糊联想是人味）。

---

## 8. 上下文编译与缓存策略

### 8.1 模块化模板与占位符组装

- **模板** = `data/prompts/{agent}.prompt.json`：有序模块数组，模块 = `{key, role: system|user|assistant, content}`，content 内 `{{placeholder}}` 占位；Web 可编辑（「提示词」页签），**每轮激活前热加载**——保存后下一轮即生效。
- **占位符注册表 = 唯一出口**：每 agent 一份 `key → provider(context) → string`；新占位符（未来的快照类，如"在场人物快照（外显）""各角色 timer 剩余时间"）= 注册新 provider，不得在组装代码里散落硬编码拼接；未知占位符加载/保存时报错。**provider 纯净化**：只输出数据本身，标题/节名/包装前缀一律写在模板静态文案里（DESIGN-P1 §10.1-12）。
- **compiler = 纯渲染器**：模板 × 注册表 × 注入上下文 → ChatMessage[]（每模块一条消息，带自己的 role）。
- **缓存友好 = 编辑约定**：动态内容（近期事件/正文滑窗/#当前场景）放尾部模块，由改模板的人自控——DeepSeek 前缀缓存跨消息边界生效，动态置尾则前缀命中。正文滑窗大小可配置：`config.json` 的 `memory.prose_window_turns`（默认 5），最近 N 轮注入正文、更早只注入事件（DESIGN-P1 §10.1-1）。

### 8.2 各 Agent 缓存画像

| Agent | 稳定部分 | 每轮变动 | 主要开销 | 缓存要点 |
|---|---|---|---|---|
| 角色 ×N | 人设 + 标签 lore + 长期记忆种子 | 近期事件（身份替换）+ 正文滑窗 + #当前场景（@CID） | 小 | 占位符现取、动态置尾（编辑约定） |
| GM | 世界设定 + lore 全文 + 演员表 | 事件流 + 工作集 + 状态快照 + 正文滑窗 | **主输入** | 同上 |
| 正文 | 基调卡 + 世界 lore + 演员表（含声纹） | 触发 lore + 近期事件 + 上一轮正文 + 本轮 GM 事件包 | **主输出** | 每轮注册表现取（§10.1-10） |
| 变量 | —（确定性代码） | deltas | ~零 | 不适用 |
| 工具 | schema + 目标文件 | 按需 | 离关键路径 | 提示词热加载下一轮生效，其余新会话生效（§10.1-11） |

### 8.3 缓存断点的落地

核心只输出后端无关的中性标记（`[[CACHE_BREAK]]` 式层边界）；翻译为 `cache_control` 等模型特定机制是边缘层（Gateway/客户端）的活（借鉴 AIRP prompt-caching.md 的分层判据）。注意 TTL 节奏（如 Anthropic 默认 5 分钟命中续期），RP 轮次节奏天然保活。（未来方向；当前 DeepSeek 纯前缀缓存，无需显式断点。）

---

## 9. 成本模型与降级模式

- 单事件窗口成本 ≈ N×角色（小输入小输出，且唤醒制下多数沉睡）+ 0~1×GM（大缓存前缀+结构化输出，攒窗口）+ 0~1×正文（中缓存前缀+长输出，按叙事窗口）。
- 对比 ST 单次巨长无缓存调用：长会话下本架构**可能更便宜**——多出的调用次数被缓存命中率覆盖。
- **降级模式（可切换）**：单角色小场景 → GM 代演 NPC + GM/正文融合一段式调用，成本逼近 ST，但保留真相层。融合模式仅作降级存在，不是架构本体（理由：成本频率错配、文风死人化实证、缓存剖面冲突）。

---

## 10. 开放问题

1. ~~打断粒度~~（已解决，见 §5.6：复用先攻判定——事件落地时先攻高者来得及收手，低者硬吃）。
2. ~~玩家异步体验~~（已解决，见 §5.7：主控进时间轴，瞬时行动由 GM 订立 timer，长期指令走快进模式双提示词）。

**当前无未决开放问题。** 层间 token 预算分配留待 M2 多角色上线后实测调优；多组交替事件对 GM 判断的影响待实际运行验证（docs/adr/0001）。

---

## 11. 路线图

- **P0（MVP，验证核心循环）** ✅ 已完成：单角色 + GM + 正文三 agent；纯逻辑模拟调度器 + 顺序运行时；事件日志 + 变量库（JSONL+JSON 文件）；CLI；缓存命中埋点。
- **P1（多角色+时间轴，进行中）**：
  - **M1 已完成** ✅：事件记录体系（@ID 占位 + 身份替换 + 统一标签 + lore 触发制）+ 提示词模板模块化/Web 编辑器/热加载 + 存档 v2（五文件 + llm-recent）+ 回溯/停止/编辑/继续/重 roll（DESIGN-P1 §10.1/§10.2）。
  - **M2（设计冻结，DESIGN-P1 §10.3/§10.4）**：**M2-a** 异步多角色核心（timer 角色变量 + 相对偏移契约 + 组派生 + 先攻连续轮 + simulator 重写 + 事件头自然语言时间地点 + `time.json` + 白滩镇 4 角色开局）→ **M2-b** 无判定轮与五标记体系 + inScene/inTalk 合并为 Group 变量 + 邀请流程 + 玩家输入格式统一 + 正文注入调整。
- **P2**：**基于标签的内容激活系统设计（核心任务）**、三种输入模式（主控/上帝视角/写作指令）、变量/数值规则与前端模板设计。
- **P3（事件协议+瘦前端）**：结构化事件流、retcon 工作流深化、工具 SubAgent 完整化、缓存策略调优。
- **P4（生态）**：MCP 工具集（骰子/生图/TTS）、ST 资产导入器（角色卡/世界书/预设，参考 AIRP-MCP-Server 工具集）、agent manifest 热加载、场景外模式（IM 接入）。

**数据层复用策略**：以世界设定为核心，部分复用 AIRP-MCP-Server 的工具思路（import_card / apply_lorebook / update_state / seal_volume / plugin 数据戒律），但存储模型改为世界中心（真相层+账本）而非角色中心。

**技术选型（P0 已定）**：**TypeScript**（Node.js；事件协议/裁决包用类型锁死，与未来 Web 前端共享类型）+ **DeepSeek**（OpenAI 兼容接口，`openai` SDK；无显式断点 → 缓存工作 = 前缀稳定纪律，命中经 `prompt_cache_hit_tokens/miss_tokens` 埋点验证）。LLM 抽象层走 OpenAI 兼容协议，Anthropic 原生适配器（显式 `cache_control` 断点）留作后续边缘层，编译器保留中性层边界抽象。

---

## 12. 参考

- 参考思路.txt（类脑社区"独立角色"实践）：角色自治哲学、第一人称认知改写、GM 克制红线、记忆三层、Audit/Diary/Chat 模式、涌现实证。
- [GhostXia/AIRP-MCP-Server](https://github.com/GhostXia/AIRP-MCP-Server)：数据层/推理层分离铁律、决策提示而非强制工作流、通用优先于特供、缓存下沉边缘（[prompt-caching.md](https://github.com/GhostXia/AIRP-MCP-Server/blob/main/docs/prompt-caching.md)）、skill vs MCP 分工（[skills-vs-mcp.md](https://github.com/GhostXia/AIRP-MCP-Server/blob/main/docs/skills-vs-mcp.md)）、隔离 subagent 治"死人化"、token 纪律细节。
- [ST-ClaudeCacheGateway](https://github.com/shanye5593/ST-ClaudeCacheGateway)（AIRP 引用）：`[[CACHE_BREAK]]` 中性标记 → 边缘翻译的思路。
