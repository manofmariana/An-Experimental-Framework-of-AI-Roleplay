# Agent-AIRP P1 设计文档：多角色与事件记录体系

> 状态：M1 已完成（§10.1/§10.2）+ M2 设计冻结（§10.3，2026-07）
> 前置：`DESIGN.md`（架构基准，本文档细化并部分修订其 P1 条款）
> 范围：4 个角色首发（含主控 C0 + 3 NPC，不设上限，埋点观察）、事件记录体系、同步组、无判定轮与标记体系、三级记忆、统一标签体系、连续时间 DES。

---

## 1. 事件记录体系（取代 P0 的双轨记忆）

**单一事件库，ID 占位，渲染时身份替换。**

- **只有 GM 可以添加新事件**（含转写角色行为产生的私密事件）。**所有角色（包括玩家）的言行不注入事件库**——未裁决的言行进入"当前轮工作集"，按结构组装后直接注入（见下），直到 GM 裁决时才被转写为事件。
- 真相层只有一套事件记录。角色不再有独立记忆流——**角色的"记忆" = 它可见事件记录的视角化渲染**（**NPC 的所有记忆都从 GM 写的事件里组装，无论是否私密**）。
- 记录格式用占位符：`@C1002 对 @C0 做了某事`（**玩家固定为 C0**，角色 ID 为 C+编号，**生成时由工具 Agent 按顺序从小到大分配**）。
- 渲染给某个读者时，代码做身份替换：自己 → "我"；认识的人 → 真名；不认识的人 → 人际关系库中的**印象称谓**（"鞋上有青苔的男人"）。
- **`known_by` 使用统一标签**（角色 ID 即角色标签）——与 Lorebook 标签同一套体系（见 §2），统一界面管理。私密事件 = 可见标签仅含自己。
- **事件头**：事件存结构化 clock 值与 location 文本；自然语言时间头（"第3日·入夜后"）在注入/显示时经世界设定集时段映射表（`time.json`）机械渲染，**LLM 不自由生成时间头**；跨场景对话事件 location 置空（感知不靠它，§2）。长期事件完成时间（若有）同样自动记录。

**注入结构**（每个角色的提示词按此组装，示意非最终结构）：

```
#过往
（从事件库按 known_by 拉取 + 身份替换组装）
（连续轮内的前 N 轮正文，注入条件见 §6）

#当前场景（当前连续轮中已做出决策的角色言行——未落库，直接组装）
时间 / 地点
##张三
  发言：…
  动作：…
##李四
  发言：…
  动作：…
```

### 1.1 同步组（派生状态）

**组不是实体、不持有任何变量——它是程序从角色变量派生的状态。** 角色变量含 `location`、`timer`、`group`、`reaction`、`initiative`、`isPlayer` 等：

- **Group 变量 = {组编号， 组位置}**：`组编号=0` = 单人组（恒有值），`[1,n)` = 多人组编号；**组位置总绑定先攻最高者的 CID**（= 其 location；领头者变更则重绑，同值取最小 CID）。inScene/inTalk 双变量已合并，代码层不再区分组类型。
- **成组 = timer 一致 + location 一致（自动）**；邀请可跨地点并入（见 §5）。构成判定完全机械化；**GM 不设组，只在裁决时设置各角色的 `location`/`timer`**——组划分表面自动、实际由 GM 决定（划分判据都是 GM 写的）。
- **入组劣后**：角色入组时自身位置 ≠ 组位置 → 先攻 -1（远程参与的代价）。
- **单活跃组不变量**：任意时刻只有一个组在前台行动（DES 串行弹出）；同一到期时刻弹出多组 → 逐组串行（按组内最高先攻值降序，同值比该成员 CID 升序），每组跑至 GM 结算为止（GM 契约：必输出非 0 timer）。后台判定纯按 timer——前台组成员结构上不可被联系。
- "组计时器统一" = GM 发往组的计时器被程序逐成员应用到各自 `timer` 变量上，故必然统一。GM 判定角色离开时独立改其 `location` 或 `timer`，该角色即离组（归 0 或入新组）。
- 感知隔离按同步组执行：密室私谈、分头行动在结构上成立（感知过滤见 §2——`known_by` 唯一通道；跨地点私密性归 P2 顶层 TAG 过滤，见 §5.6）。
- `location` 是 GM 裁决时赋予的自由文本（以世界设定集为参考、提示词管理）：GM 在全员行动完后推理，清楚谁剧情上在一起，会主动安排相同 location；不建地点枚举表。
- **组 id 指派并保稳**：成员集精确匹配 → 保留原 id；纯减员 → 剩余成员继承原 id（分裂则最大子集继承，并列取最小 CID）；纯增员 → 也继承原 id；合组 = 人少并入人多。不得由 hash(location+timer) 派生——连续轮每圈重设 timer 都会换 id，连续场景判定即垮。

### 1.2 行动轮模型（M2 定稿：计时器 DES + 组派生 + 先攻连续轮）

**每个角色是携带自己计时器（`timer` 变量）的独立对象；时间轴 = 全体角色 timer 的投影——调度 = 扫描取最近到期者。**

- **异步域**：调度器取 timer 最小的角色（组）激活；时钟跳转到该到期时刻。长期行动（闭关、赶路）= 长 timer，触发即完成事件。**timer 只能由 GM 订立**；角色决策不含 span。
- **timer 契约 = 相对偏移**：`timer: [{cid, span:{y?,m?,d?,h?,min?}}]`，程序 `due = clock + toMinutes(span)`、钳制 span ≥ 0（`{min:0}` = 立即）。GM 看到的是渲染后的自然语言时间，写绝对时刻只能瞎猜，相对偏移结构性避免负值/算错；跨轮对齐由注入"各角色 timer 剩余时间"的派生快照解决（占位符 provider 计算）。
- **同步域**：`group.组编号` 相同（≠0）的组以**行动周期**运行——纯回合制，周期内所有成员各行动一次，后行动者经 #当前场景 注入先行动者言行；**GM 不再每圈必激活**，触发规则见 §5（标记立即激活 / 硬保险周期末激活）。
- **先攻（initiative）= d20 + reaction**：代码侧确定性投掷，**总值同值即"同时"**（同时行动者互不注入——同时性的迷雾）；结果写入角色变量，结构为 **{先攻值， 组编号}**——组编号变化即重置（归 0 除外：保留旧值供召回复用）；入组时组编号对上 → 复用，否则单独补投插入既有顺序。reaction 是可成长变量，开场全员同值。
- **连续场景 = 同一个同步组（同一组编号存续期间）的全部轮次**（§6）。
- **时钟模型**：最小单元 1 分钟，记录格式 {y/m/d/h/m}；渲染层近似连续（`time.json` 时段映射表机械渲染"第N日·{时段}"），逻辑层离散（时钟只在弹出最近 timer 时跳转）。**耗时（duration）概念已废除**——计时器是唯一时间跨度设置单元（`docs/adr/0001`）。
- **玩家同构**：主控角色（`isPlayer=true`，M2 缺省 C0）与其他角色变量结构完全一致；`await_player` 按 isPlayer 判定，不硬编码 CID——为切换扮演对象、多人操控预留。

---

## 2. 统一标签体系

- **一套标签，两个用途**：事件的可见性（`known_by`）与 Lorebook 的激活**共用同一标签系统**——角色 ID 本身就是角色标签，可以给 Lorebook 打角色标签（该角色可见/专属的知识），也可以给事件打角色标签（可见性）。
- **事件侧只使用 `known_by` 一类 tag**：GM 标记时已推理各角色可视情况（谁在场、谁参与对话、私密只标自己）；感知过滤 = `known_by` 唯一通道，无地点成分、无其他限制 tag（`docs/adr/0002`）。
- **统一界面管理标签的删改**（世界设定 UI）。
- **扁平字符串 + 冒号分层约定**（如 `A国：常识`、`职业：圣骑士`、`信仰：塞伦涅`）；层级深度可按世界观调整。
- 角色固定携带标签：manifest 出身/职业等（"出身 A 国"→ 自带 `A国：常识`，自动激活所有匹配 lore 条目）；**GM 可在游戏中授予/摘除标签**（转职、获得知识、状态变化），属于 GM 转写职责的一部分。
- Lorebook 条目的写入与标签维护是**工具 Agent** 的职责。

## 3. 人际关系库（每角色一份）

- 身份替换的解析后端：认识的人 → 真名；不认识 → 库中填写的印象称谓。
- **角色全权维护，GM 完全不参与**：角色在决策输出中自行登记/更新印象与名字（报名字/相识事件由角色自己消化）。

---

## 4. 三级记忆结构（GM 与角色同构）

以 50 轮为例（1–30 轮已压缩）：

```
[长期记忆]   1–30 轮的压缩条目——各对象从自己的压缩库调取（差异化自然语言文本）
[近期记忆]   30–45 轮的具体事件——角色从公共事件库按 known_by 拉取 + 身份替换渲染；
             GM 拉取全部
[当前场景]   最近 ~5 轮正文——注入有条件（见 §6 连续场景判定）
```

- 角色也注入正文：防止细节丢失与出戏（已确认的权衡）。
- 正文**不进事件日志**（避免双份真相），但正文作为"第四级存储"承担注入素材职能。

### 4.1 记忆压缩

- **专用记忆压缩模型**，**每个对象独立压缩**（角色各自的库 + 公共库）。
- **只追加新条目，不改写旧条目**；条目膨胀不设防——**由 RAG 检索兜底**（注入时检索相关长期条目而非全量）。
- 压缩后原始事件**不删除**，只是不再注入、以长期条目替代（真相层永不丢数据）。
- 压缩单位："压缩尚未被压缩的最远 N 层的事件"。
- **P1 先手动激活**；按轮次/上下文阈值自动激活的方案以后再定。
- （细致的分 agent 提示词规划不是当下目标；注入组装管线的构想见 §4.2。）

### 4.2 注入组装管线（图1 构想）

- GM 的世界作用分两路：**直写事件库** / **add·delete·update 操作**（变量）。
- 注入**全自动组装**：正文 agent、角色 agent 各自按记忆结构窗口（§4）从事件库取材，经身份替换渲染后注入——无手工拼接。
- ⚠️ 待澄清：图1 中"经过正文形成事件库"一行与"正文不进事件日志（防双份真相）"的既定原则存在冲突，下次讨论确认含义。

---

## 5. 无判定轮与标记体系（M2-b 定稿，取代对话轮）

核心：**无判定是轮的默认形态，GM 激活是例外。** 一个行动周期内无人立 GM 请求标记，整个周期即无判定——角色照常输出台词与行为（不再强制只输出对话，二者有其一即可），GM 不激活。擦桌子、抬头看一眼这类行为不再需要 GM 判定；对话也不再需要专门的发起机制——说话就是普通行动。

### 5.1 行动周期与 GM 触发

- **行动周期（action cycle）** = 连续场景内所有成员各行动一次。硬保险"GM 强制间隔"以行动周期计。
- **GM 激活两种触发**：
  1. **标记触发**——周期内有人立 GM 请求标记 → **立即激活**（不等周期末——开门类判定不能等，否则其他角色会基于未裁决状态继续行动；但**同先攻批必须全员行动完才激活**，批内同时性不被裁决撕裂）；
  2. **硬保险**——距上次 GM 激活满 N 个行动周期（**N 存 config.json，设置页可编辑**）→ 周期末激活。
- **计数器不落盘**：`cycles_since_gm` 从 archive seq 序列派生（自上一 GM 步以来的完整行动周期数），回溯零特例。**玩家输入计入行动周期计数**（isPlayer 不特判）。
- **立即激活的 timer 覆盖契约**：中途 GM 的 timer 必须**精确覆盖全体同步组成员 + 刚从同步组离开的成员**（程序强校验：缺少/越界/重复即拒绝重试）。覆盖未行动成员是周期补完的前提而非打断——**周期序列与 timer 无关，是组自身的状态**（acted 三规则：行动后→已行动；全组已行动→全员重置、X+1；组结算进后台→全员重置、X 归 0）；span 一致则组状态不变、未行动者保持未行动、周期不乱，不设才会让 span 不一致、被 reconcileGroups 编入别组、打乱周期。组编号因 timer 同步存续，行动顺序由 {先攻值，组编号} 派生还原（同值批内按 CID 升序）。
- **中途 GM 必须设 timer**（中途触发可能本身就是离场判定）；GM 契约：**必输出非 0 timer**（成员推入未来后本组自然让位）。
- **行动顺序表**（取代 SEQ 差规则）：顺序 = 派生（initiative 变量现排，只有前台组用）；**已行动位 = 角色变量**（快照落盘、var_changes 落账，行动置位、周期完成/后台重置清零，不做 archive 回扫）；**周期计数 X = 世界变量**（完成 +1、GM 激活清零、达 N 周期末 GM）。**周期补完**：前台组不变 ⇒ 已行动状态不变、未行动者继续直至全员行动完（中途 GM 不动已行动位，无特例）。

### 5.2 标记（Marker）

角色/玩家输出中的结构化指令位：**DecisionPackage 可选 `markers` 数组**，每条 `{type, ...params}`——`gm_request` / `leave`（互斥）/ `recall{target}` / `contact{channel, targets[]}` / `confirm`。程序只读结构化字段、不做行内文本解析；提示词按结构化格式教学。**标记不进工作集、不进任何注入**——解析后只有即时程序作用（调度/组别/timer 变更），即抛。

- **GM 请求**：请求裁决；与离开标记二选一。触发规则见 §5.1。
- **离开**：离场**不触发 GM**——程序当场将其组编号归 0、timer 置超大值（**所有角色含玩家统一程序化**）；结算（离场事件、新 timer）推迟到下一次 GM 激活。离场者的过去言行继续注入原组直到 GM 清算；后续角色经 `{{departure_notices}}` 占位符获知离场与召回方式。
- **召回**：把已离开且未结算者拉回——timer 重新归零（即组原到期时刻）、按进组规则归组，**先攻不变、发言顺序保持**（{先攻值，组编号} 对上即复用）。**跨周期有效**（离开 → GM 结算前均为"未结算"）。
- **联系**：发起跨地点邀请（途径字段 + 对象列表），机制见 §5.3。
- **确认**：被邀请者接受邀请（+ 首轮回复）；拒绝则输出理由、不立此标记，timer 自动还原。

离开/召回等经过的可见性由未来的注入渲染机制承担（推迟到提示词组装设计阶段，与 P2 TAG 过滤一并设计；M2-b 落地时无渲染）。

### 5.3 邀请（拉人入组）

- 由联系标记发起（途径字段：电话/视频/远程开门……）。**目标规则：至少含一个异地角色**（不再局限于异地）；**本地被邀请者（与发起者同地点）默认同意**、无需应答激活，异地被邀请者按常规顺序应答；目标只能是**后台角色**（前台活跃组成员结构上不可被联系，后台判定纯按 timer）。
- **延迟生效**（防止时钟抢跑：邀请发生在 N 个周期后，立刻归零受邀者 timer 会抢在其旧组行动之前）：联系标记**触发 GM 立即结算**（GM 与受邀者都看不到邀请标记），邀请在**邀请者所在组下一次回到前台时**才激活异地受邀者（timer 置 0 弹出应答）；若其他组 timer 更早（可能是受邀者旧组），他们先行动。
- **拒绝**（附理由）→ timer **自动还原**为被邀请前的值，不调用 GM，并失去频道变量（全体频道持有者因此同地 → 频道自动清除）；**接受**（确认标记 + 首轮回复）→ timer 确实归 0 直到 GM 重设，并入发起者所在组（位置 ≠ 组位置 → 先攻 -1），**首轮回复计入本周期已行动**。
- **频道变量（channel）**：联系标记生效即分配给邀请者与全部对象（同一 id）；拒绝者失去；退组自动清除。职责二合一：**防重入**（频道持有者不进任何联系人列表注入——通话中/邀请建立中不可再被邀请；持有者/待答邀请者自身也看不到名单，占位符整体置空）+ **生命周期**（任意两个持有者 location 不同 → 跨场景存在；全同地 → 全清，非组位置持有者离场等结算）。
- **回归原组不需要专门机制**：远程成员 location 未变，GM 依其行为（如挂断电话）把 timer 对齐回原组到期时刻，同地同时自动成组即回归（组 id 保稳延续原组身份）。
- **注入标注**：位置 ≠ 组位置的成员在注入中显式标注（远程参与）；计时器对齐靠提示词约束 + 近期事件注入。

### 5.4 时钟模型（无特例）

时钟只在调度器弹出最近 timer 时跳转——组运行期间（无论多少行动周期）时钟冻结在当前到期时刻，这是 M2-a 既有模型的直接推论，**不再有"对话期间世界暂停"特例**。长期行动（闭关、赶路）= 长 timer；突发鉴定可打断（§10.3）。

### 5.5 正文只能被 GM 激活

- 无判定轮内玩家直接看角色卡片言行流（零正文成本）；GM 激活后，正文 agent 把 GM 事件包 + 各角色台词内心一次性渲染成小说段落。
- 正文注入中 GM 事件包**之前**是：本轮各角色的**台词+内心**（**不含行为意图**——意图是裁决元信息，不是小说素材；**标记也不注入**）。

### 5.6 私密性与 TAG（P2 衔接）

跨地点私密性（一边酒馆闲聊、一边电话悄悄话）**不在组层做**——同处一组，由 P2 的顶层 TAG 过滤系统分流：所有注入组装前先经 TAG 过滤，GM 按判断生成带 tag 的事件，能否收到取决于角色是否具有对应 tag。M2-b 期间组内注入全透传（AV 标签位方案已撤回）。

### 5.7 玩家输入统一（M2-b 必做）

主控输入格式与 NPC 输出统一：输入框拆三块（**台词/行动/内心（含意图）**）+ 标记按钮（五种标记），前端组装为与 NPC 完全同构的 DecisionPackage JSON。契约：inner 必填（intent 已并入 inner），**action 与 dialogue 至少其一**。GM 可见全部字段；正文只取台词+内心。**暂停选项**（取代默认自动继续）：自动继续 / 每轮暂停 / GM 前暂停 / GM 后暂停 / 正文后暂停——前两项各自与其他互斥，后三项自由组合；服务端按会话持有，localStorage 持久化。

---

## 6. 连续场景判定（正文注入条件）

**连续场景 = 同一个同步组（同一组编号存续期间）的全部轮次。** 组解散/重组（timer 或 location 被独立修改）即场景断点，断点两侧为非连续场景。

- **事件本身携带角色标签**；**有事件不一定有正文，但有正文一定有事件**——轮次一一对应，不设独立的正文标签。注入时按该轮落在哪一级决定素材：长期（各对象压缩库）/ 近期（事件）/ 最近几轮（正文）。
- **正文归档元数据**：每个正文结果记录 `participants: string[]` 与 `scenes: Record<CID, 组编号>`，其中场景值取该轮行动时、GM 改组前的组编号；正文内容本身仍不是事件真相。
- **角色的正文注入条件**（两个同时满足）：`participants` 含该角色 CID ∧ 归档 `scenes[CID]` 与其当前组编号相同；按该过滤结果取最近 N 段，禁止跨角色或跨组正文进入角色上下文。
- **GM 的正文注入条件**：本轮行动者中至少一人满足上述连续场景条件；只注入与当前轮连续的正文，禁止其他组正文混入。
- **正文 agent 永远全量注入**（近期正文不断档，保证文风稳定）。
- **正文渲染范围 = GM 包全部事件**（不按玩家视角过滤；多组交替 = 蒙太奇叙事）；限定视角（第一/第二人称）未来经 `known_by` tag 过滤即可扩展。narrativity 为包级，skip 则整段无正文。

---

## 7. 角色生命周期

### 7.1 新角色的两个来源

- **预设人物**：从 lorebook 提取或已按格式存储的 manifest。
- **热生成**：**完整经历生成管线**——工具 Agent 先生成角色框架，然后工具 Agent 与该角色 agent 进行数轮相互询问，按年龄分段生成过往经历（童年/青年/成年）与近期经历，**直到对齐当前时间**。
- 公共事件/舆论不作为事件注入新角色，而是生成或补充到对应标签的 lorebook 条目内（如 `A城：公共`），走标签激活通道。

### 7.2 创建权限与确认

- **只有 GM 有权激活 agent 创建程序**；但每次激活创建**必须经玩家确认**。
- 创建意图可多元产生：剧情需要、NPC 带来朋友、玩家元指令。
- **严格控制主要角色数量**：GM 将被注入严格的 NPC 创建准则（玩家行动一次等十几个 NPC 行动是不可接受的）。次要 NPC 不允许随意升级为主要角色。

### 7.3 三种输入视角（R6 重定义，取代"元指令"）

玩家输入分**三个独立输入窗口**，本质是将输入注入到不同位置：

| 输入 | 内容 | 注入位置 |
|---|---|---|
| **主控视角** | 玩家扮演主控角色的言行（最基本形态） | 事件日志（known_by 按在场组）→ 各角色感知 |
| **上帝视角** | 玩家控制整个叙事（"命运传来了指令"） | NPC 提示词（命运段落）+ GM（该轮 ban 掉 NPC 注入） |
| **写作指令**（原"元指令"） | 与故事内容无关的**当轮写作要求**（文风、详略、视角等），介于系统层与叙事层之间 | **仅 GM**（GM 负责是否激活正文、正文字数等执行） |

**共存规则**：写作指令可与主控或上帝视角共存；**主控与上帝视角每轮二选一**。

**上帝视角轮的语义**：
- NPC 提示词加入命运段落，示例结构：`#当前情况`（对话/场景）+ `#命运传来了指令，未来发生的一切已经注定，无论行为或内心，你必须按照以下指示中的内容做出反应`（上帝输入内容）。
- 该轮 **NPC 的输入不再注入 GM**；NPC 活动的唯一目的是**为后续轮次提供连续的心理活动和目标**（按命运指令更新自己的内心）。
- GM 完全依照上帝叙事输出：调整变量、输出事件摘要（供正文使用）、按写作指令做特殊变更。
- 角色红线在此让位于显式的"命运"机制：上帝视角是系统级的强制通道，不是 GM 自作主张替角色决定。

**可见性**：三者只是注入位置不同，从对话历史直接可见——不做专门日志。

### 7.4 玩家计时器与轮空

- **玩家的输入也要等自己的轮次**（玩家有计时器，与其他角色同构）。
- **轮空计数 + 可调节的最大轮空阈值**：超过阈值时，NPC 提示词切换为"请输出你未来（某段时间）——（对齐玩家计时器到期）的规划"。
- **GM 给 NPC 上计时器时不得早于玩家计时器到期**（保证玩家不被世界甩下）。一般情况下玩家轮空不多。

### 7.5 退场 trivial 化

- agent 无运行态（每次调用 = 从本地资源抽取结构化注入的独立请求），所以**临时/永久退场无需区分**：不激活即退场，角色状态就是存储数据本身。

## 8. 规则引擎与变量系统（方向性）

- **判定依赖规则而非 LLM**：伤害、检定、机制结算由确定性规则引擎执行（可扩展新机制——如"真实伤害"作为新伤害类型注册进规则）；GM 负责根据检定/结算结果编事件、改世界变量，不做数学。
- **角色决策包**：`inner` 必填（内心活动与意图，intent 字段已并入）；`action` 与 `dialogue` **至少其一**（皆可选）；关系实际变化时追加 `relations`；调度指令追加 `markers`。角色 `span` 已删除，`SpanSchema` 仅供 GM `timer` 使用。
- **未来角色可写的部分变量**（如装备、消耗品）以明确的决策包可选字段表达，经确定性的 CharactersStore 与 `var_changes` 通道落账并可回溯；不引入通用 `changes` 字段，GM 不作为这些角色自有变量的唯一写入者。
- **变量系统按存档/世界观自定义**：由工具 Agent 生成与维护（不同世界观不同变量集）；**状态栏的维护与渲染**由工具 Agent 与玩家商讨生成。

## 9. 首发配置与成本

- 首发场景 **4 个角色（含主控 C0 + 3 NPC）**——开局构图见 §10.3；不设激活上限（先不装护栏，用埋点数据观察）。
- 无判定轮使 GM 调用频率从"每圈一次"降为"按需（标记触发 / 硬保险）"；非连续场景的时间跳转进一步压缩正文渲染次数。

## 10. 实施分期与工程决定（R8 定稿）

**分期**（利用提示词模块化组装的优势，不影响机制的东西后置）：

- **M1**：事件记录体系改造（@ID+身份替换、统一标签、#当前场景工作集、三级记忆注入结构）——**直接替换 P0 行为，不留兼容路径**。（✅ 已完成，实施决断见 §10.1）
- **M2**（设计已冻结，§10.3）：分两刀——
  - **M2-a（异步多角色核心，无对话）**：角色变量扩展（timer/inScene/inTalk/initiative/reaction/isPlayer）+ GM 新裁决包契约 + simulator 重写为派生纯函数集 + 多角色主循环（在场组连续轮 + 先攻）+ 动态 phase 与回溯适配 + 事件头自然语言时间地点 + `time.json` + 白滩镇 4 角色开局。
  - **M2-b（无判定轮与标记体系）**：无判定轮（GM 按需激活：标记立即/硬保险周期末）+ 五标记契约（gm_request/leave/recall/contact/confirm）+ inScene/inTalk 合并为 Group 变量（组位置绑定先攻最高者）+ 邀请流程（跨地点入组）+ 玩家输入格式统一（四块 + 标记按钮）+ 正文注入调整（§5.5）。
- **延后（P2）**：基于标签的内容激活系统设计（核心任务）、三种输入模式（主控/上帝视角/写作指令）、变量/数值规则与前端模板设计。记忆压缩+RAG 归入标签激活系统议题。

**工程决定**：

- **存档结构**：~~角色私有数据按角色分文件（`runs/{id}/agents/{cid}/`）~~ **已被 §10.2 存档 v2 取代**——五文件（events/lore/world/characters/archive）+ llm-recent/，角色数据并入 characters.json 单文件（详见 §10.2）。逻辑上 agent 只是结构化组装提示词，存储按数据所有权分即可。
- **世界设定集可选**：每个存档创建时可选择加载哪套世界设定（不再只有默认一份）；示例世界基于白滩镇扩展为 4 角色（含标签、初始人际关系、reaction 属性、带标签 lore）。
- **当前轮工作集落盘 + 连续轮自动收尾**：见 §5.5。

**仍待定**：压缩的自动触发策略；快照 JSON → 自然语言渲染器的具体模板；成本埋点看板。

### 10.1 M1 实施决断（定稿，已落地）

M1 已完成（直接替换 P0 行为，无兼容路径）。实施中定死的决断：

> 读法：各条按时间顺序记录实施决断，相互覆盖处以序号靠后者为准；存档文件细节以 §10.2 为准。

1. **正文滑窗动态注入**（推翻早期"正文 append-only 全量注入"方案）：提示词组装为 `[L0][L1（含长期记忆种子）][近期事件 append-only L4][正文滑窗 window][L5]`。缓存命中分界点在"近期事件"与"N 轮正文"之间——正文窗每轮变化只影响尾部。**window 段是唯一豁免 append-only 断言的段**（仍守时间戳/轮次禁令）。窗口大小可配置：`config.json` 的 `memory.prose_window_turns`（默认 5）——未压缩轮次中最近 N 轮注入正文，更早的只注入事件。注入条件守 §6：角色 = 该轮参与者含其 CID ∧ 连续；GM = 连续；正文 agent 永远全量（维持 append-only L4）。M1 无计时器跳转、恒连续；每轮记 `participants`/`continuous`（§10.2 后改记 archive.json），连续性判定接口已留给 M2。
2. **GM 转写**：玩家输入 + 角色决策包进入当前轮工作集（working-set.json 落盘保留）；GM 裁决时转写为 @ID 占位事件（M2 起为**摘要制**：不完整转述台词，做摘要与意义总结，纪律见 §10.3）。角色台词不再由系统单独 commit。正文 agent 输入 = 组装后的角色全部响应（#当前场景：台词+行动意图+内心）+ GM 转写事件。**一轮只产出一个事件**（冒烟后修订）：`AdjudicationPackage.event` 为一整段自然语言，既作正文参考、本身也是本轮摘要；`narrativity` 为单值（full/brief/skip）。（M2 修订：事件数跟随新组划分，见 §10.3。）
3. **M1 主循环绕过 simulator**：loop 直接编排 character → GM → prose。simulator.ts 保持纯逻辑（仅跟随 Event 契约做 `known_by` → `tags` 的机械适配），M2 计时器/DES 上线时回归。
4. **统一标签落地形态**：Event 的 `known_by: string[]` 字段废除，`tags: string[]` 取而代之——可见性 = tags 含 `known_by:{读者CID}`（与 Lorebook tags 同一套体系）。AdjudicationPackage 不设 known_by 记录，GM 可选输出 `event_tags: ["known_by:C0", ...]`（单个可选数组，冒烟后由按索引 record 修订），缺省 = 本轮全部参与者的 known_by 标签；私密事件 = 标签仅含自己。**GM 的 lorebook 全文注入 L1**（id+tags+content，按 ID 排序；冒烟后由索引改为全文，GM 基于全文选择 active/forbidden）；角色固定标签（manifest.tags）自动激活匹配 lore 注入 L1。
5. **ID 分配**：玩家固定 `C0`；示例角色 `C1001`（M2 工具 Agent 顺序分配机制占位）。GM/正文/角色的 L1 注入演员表（@CID ↔ 真名/称谓），事件在 L4 保持 @ID 原文；角色侧经人际关系库做身份替换（自己→我/真名/印象称谓/陌生人）。
6. **memorize 退役**：角色不再有独立记忆流，memory.jsonl 废除。DecisionPackage 删 `memorize`、增 `relations`（人际关系库更新，角色全权维护）；库存 `runs/{id}/agents/{cid}/relations.json`（§10.2 后并入 characters.json）。manifest 的 `initial_memories` 保留为 L1 长期记忆种子。
7. **事件头自然语言时间地点：M1 未做，M2 必须补上**（世界时钟 → 自然语言时间头是整个世界观机制的一部分，连同在场组/计时器一起落地；M1 仅保留结构化 `location` 字段）。（M2 方案已定：time.json 时段映射表机械渲染，见 §10.3。）
8. **世界设定集**：`data/worlds/{setId}/`（setting.md / tone-card.md / lorebook.json / characters/*.json），示例集 `baitan`；新会话可选（WebUI 下拉 / `new_session.world_set`），选择记入 `runs/{id}/meta.json`，resume 按 meta 加载（缺省 baitan）。（M2 增 `time.json`，见 §10.3。）
9. **提示词模板模块化 + Web 编辑器（提前落地，原排 M2）**：模板 = `data/prompts/{agent}.prompt.json`（模块 = {key, role, content}，`{{placeholder}}` 占位），`src/prompts/` md 已废除。**占位符注册表是唯一出口**（每 agent 一份 `key → provider(context) → string`，未来快照类占位符——如"在场人物快照（外显）"——经同一路径接入）；未知占位符加载/保存时报错。**缓存铁律断言整体退役**：compiler 退化为纯渲染器（模板 × 注册表 × 上下文 → 消息数组），缓存友好降级为编辑约定（动态内容置尾），时间戳/轮次禁令取消（时间、地点经占位符注入，M1 为结构化世界时间 T，自然语言时间头仍属 M2 第 7 条）。Web 编辑器 = 「提示词」页签 + `GET /api/prompts`、`PUT /api/prompts/{id}`、`GET /api/prompts/placeholders`（目录从注册表自动导出）。
10. **lore 触发制（取代 GM 选 lore）**：AdjudicationPackage 不含 `active_lore`/`forbidden_lore`——GM 不为正文激活/禁知条目（其 lore_full 注入保留作自用参考）。正文的 lore = **本轮在场角色固定标签（manifest.tags）经 getByTags 激活的并集**（去重、按 ID 排序）；**秘密类条目的访问控制自然落在标签上**（如 `灯塔：秘密` 只有持该标签的角色才触发它进正文）。正文注入格式：协议（含基调卡）→ 世界设定 → 近期事件（演员表渲染）→ 演员表（CID 与名字）→ 触发 lore → 上一轮正文 → 本轮各角色台词与内心（不含 action/intent）→ 本轮 GM 事件包 → 输出格式；正文不注入 GM 公共长期记忆或角色声纹，每轮内容由注册表从数据层现取。
11. **提示词热加载**：每次 agent 激活前从磁盘重新 loadTemplate（小文件直接读，不做 mtime 缓存）；PUT /api/prompts 后下一轮调用即生效，不再走 markStale（config/world/character 等其他资源维持"新会话生效"规则）。
12. **占位符纯净化与按 cid 取数**：provider 只输出数据本身——标题/节名/包装前缀（【场景回顾】【事件】【上一轮正文】【本轮裁决包】等）全部写在模板静态文案里。#当前场景一律用 `##@CID` 标注（不使用名字/称谓）：角色的认知走人际关系库，不从此通道泄露。**角色模板不注入演员表**（演员表带真名会让角色不劳而获地知道陌生人名字，污染人际关系库——名字必须靠剧情获得、由角色自己登记 relations；cast provider 保留注册供 GM/正文用）。角色上下文 = 目标 cid + 全量数据层引用（manifests Map 等），provider 内部按 cid 解析——所有角色共用同一 character.prompt.json，M2 加角色零改动。**长期记忆按对象分库**：角色各自 `agents/{cid}/longterm.json` + GM 公共库 `agents/gm/longterm.json`（压缩是 P2，M1 仅建存储位）；**正文固定取 GM 库**（长期记忆按对象特化压缩，正文用 GM 压的那一版），空则模板模块丢弃；角色取自己的（M1 = initial_memories 种子）。

---

## 10.2 存档 v2 与回溯设计（定稿；本轮实施第一步 A = 存档结构改造）

### 总轮次 seq

- 总轮次计数 = 每次 API 调用 + 玩家输入（玩家轮也算一个 seq）。M1 固定调用顺序（玩家→角色→GM→正文），一轮 = 4 个 seq。
- **未来工具 Agent 调用不计入 seq**：工具 Agent 有独立计数器，与 seq 一一对应记录；工具 Agent 的修改（变量系统/提示词/占位符结构）不能用普通回溯回滚，但**回溯跨越含工具 Agent 结构变更的 seq 时强制绑定回滚**（P2+ 实现）。

### 文件规划（runs/{id}/ 下五文件 + llm-recent/）

1. **`events.json`** — 当前事件集（替代 events.jsonl）。GM 输出事件后**立刻写入**（这本身就是归档）。Event 增 `seq` 锚；纯函数 `truncateEvents(events, seq)` 截断到指定 seq（回溯用；回溯丢弃不留底，无 graveyard）。
2. **`lore.json`** — `{entries, changelog}`：新会话创建时把世界 lorebook **拷入存档**，档内增删改只动副本（防污染原始 data/）；changelog 逐条记录严格可逆变更（add/delete/update 均带 before/after + seq 锚）；纯函数 `rollbackLore(file, seq)` 按 changelog 从当前逐轮反向回滚。GM/角色/正文的 lore 注入全部读档内副本。
3. **`world.json`** — `{schema_version, world:{time:{y,m,d,h,min}, ...世界内容}, pipeline:{seq,phase,working_set,current}}`。不落盘 `clock`/`vars`；clock getter 从 world.time 按 y=365d、m=30d 派生，推进记录可逆 `world.time` VarChange；pipeline 永不进入 agent 的 world snapshot。
4. **`characters.json`** — `{schema_version, characters:{cid: CharacterState}}` 单文件。C0 与 NPC 同构，字段为 name/gender/age/personality/tags/reaction/location/timer/group（{组编号， 组位置}）/initiative（{先攻值， 组编号}）/level/isPlayer/relations/long_term_memory/vars；无 persona/voice、无 `gm` key。角色只注入自己的完整快照，GM 注入全部角色快照，正文不注入 GM 公共长期记忆。
5. **`archive.json`** — `{schema_version, entries:[...]}` 归档（单文件不拆分）；正文 result 必须持久化 `participants` 与 GM 改组前的 `scenes`，编辑正文时原样保留。元素 = `{seq, kind, result, edited?, var_changes}`。kind：`player`（**玩家轮**）/ `character:{cid}` / `gm` / `prose`。result = 该轮最后一次修改后的结果（原始返回+解析包；`edited` 布尔位留给手动编辑）。**var_changes = 本步的逐条变量变更 `{path, before, after, before_exists?}`（瘦身替代完整 var_snapshot）**——记录点覆盖 state_deltas 应用（`vars.*`）、clock 推进（`clock`）、relations 更新（`{cid}.relations.*`）、长期记忆 append（`{cid}.long_term_memory`）；手动编辑触发的效应走同一通道。**回溯 = 倒序反向执行**（先反向当前步，再逐条弹出 archive 中 seq > N 的条目把 before 写回，路径新建经 before_exists=false 删除并 prune），回滚后变量与第 N 步结束时逐字节一致。var_snapshot 时代设计备注：角色变量中的出身/人设/长期记忆只是暂存变量系统，将来会与 lore 合并进一个新的**条目管理系统**（P2 议题），当前结构不做预防性重构。**写入时机：只记录到上一轮——下一步启动时立刻把上一步写入归档**；进行中步骤的结果暂存 world.json 流水线 current 区。
6. **`llm-recent/{agent}.json`** — 每对象一个文件（此处允许），只存该对象**亲身参与的最近 5 轮** `{seq, messages, reasoning}`（滚动窗口）。替代 runs/{id}/prompts/ 逐轮散文件（已删除）。前端 live 轮的"提示词/思维链/原始返回"读前端流式缓存；历史轮按钮读 llm-recent，超出窗口显示"已轮换出窗"。

### 其他

- turns.jsonl 废除：历史回显、上一轮正文、正文滑窗全部改从 archive.json + world.json 当前轮取。
- 旧存档一律不兼容（延续原则）。缓存埋点 cache-stats.jsonl 保留原样（turn 字段 = seq）。
- GM 单事件契约、lore 触发制、占位符注册表、热加载等 M1 机制全部保留，只是数据源换到新文件。
- 每轮流程：`seq++ → 归档上一步 → 执行本步 → 结果写 world.json 流水线 current →（GM 步）事件立刻写 events.json`。
- 第二步 B（回溯/停止/编辑/继续/重roll + UI）已完成，见下节「B 步落地」。

### B 步落地（回溯/停止/编辑/继续/重 roll，已完成）

- **回滚不变量**：`rollbackTo(targetSeq)` = 回到第 targetSeq 步刚完成的位置——**倒序反向执行变量变更**（先反向当前步，再逐条弹出 archive 中 seq > targetSeq 的条目把 before 写回；路径新建经 before_exists=false 删除并 prune 空父层，schema 保证存在的顶层键保留）；archive 截到 targetSeq-1、弹出的第 targetSeq 条转回 `pipeline.current`（归档只到上一步、current = 最新步）；events 截到 seq ≤ targetSeq；lore 按 changelog 反向回滚；phase 按固定顺序（player→character→gm→prose）从弹出步 kind 推断；工作集从当前轮（最后一个 prose 之后）的 player/character 步重建；agent 内存态按截断后事件集重置。llm-recent 不动；回溯丢弃不留底（不可回"前进"）。回滚后变量与第 targetSeq 步结束时**逐字节一致**（测试断言）。
- **interrupted 语义**：停止（AbortController）后当前步标记 `interrupted`，部分原始文本保留在 `current.result.raw`，界面冻结在暂停态；**此状态拒绝 continue**——必须回溯或编辑。
- **编辑-继续合并**：editResult 经 zod 校验（决策包/裁决包须合法 JSON，正文纯文本直接过），写回 current.result 并置 `edited`、清 interrupted；**interrupted 步编辑时补做该步完成效应**（角色=工作集+relations，GM=deltas/时钟/事件 commit/感知，正文=工作集清算）。**已完成步也可编辑**：**角色步编辑 = 程序重读整个输出并完整处理**——按步落账时记录的 `effects_from`（setup 之后效应在 var_changes 中的起始下标）整段倒序反向旧效应（relations、acted、邀请应答的 confirm/拒绝效应含对邀请者等他角色的改动、标记效应），再按编辑包重放 relations → 邀请应答（步落账时另记 `invitation:{contactSeq,inviter,channel,preInviteTimer}`；非应答步重放 acted=true）→ 标记执行；无 `effects_from` 的旧步按 markers_from 尾段 + relations 路径过滤退化；**GM 步 = 旧效应整体反向（变量倒序 + 事件按 seq 截断）后按编辑包重放**——事件替换提交、各 agent 内存态按新事件集重建（回滚到 GM 步后编辑亦同）；正文步只改记录（participants/scenes 原样保留）。解析失败不落盘。
- **重 roll** = rollbackTo(seq-1) + continuePipeline（回到该步之前重跑该步及后续，确认后丢弃）。
- **状态直编（Web 状态栏，M2-b 增补）**：`PUT /api/session/state` → `GameSession.applyDirectEdit`——world/characters/events 三域 zod 校验后整体替换（原子性：任一失败整体还原）+ 各 agent 内存态按新事件集重建；**LLM 在途（busy）拒绝**；角色集合只改内容不增删；phase 由 deriveNext 重推**并广播 pipeline**（前端输入权限立即跟随，机制变量编辑即时影响下一段派生——派生本就现算无缓存，曾经的"不生效"是 pipeline 未广播的前端陈旧）；输入与 phase 不匹配时按最新派生立即拒绝并报明当前等待对象。**变量域并入当前步 var_changes（净额合并：`src/truth/varDiff.ts` 叶级 diff → 同路径改写末条 after、无记录尾部追加，不删不重排以保住 effects_from/markers_from 下标）——archive 记的是跨步净变更**（LLM 调 0→100 后手调 50，归档记 0→50，手动编辑不是独立变更记录），**回溯 = 回到对应轮最后状态（进入下一轮之前，含手动编辑）**；事件域不走 var_changes，回溯按 seq 截断的既有口径不变。**state/events 推送时机**：直编后 `onStateRefresh` + 每个步边界（input/continue/reroll 的 turn_done、rollback、edit_result）+ new_session/load_session + WS 重连同步（`sendStateEvents`，流式 delta 期间不推）；前端侧栏按当前视图（sideView）实时重渲，直编模态预填缓存由此保持新鲜（会话切换清缓存防跨会话陈旧）。
- **暂停态权限框架**：phase ≠ await_player 时主控输入禁用（"世界进行中，可继续或回滚"）；interrupted 时提示编辑/回滚。**上帝视角/写作指令输入窗是 P2 功能，本轮只留权限框架不接窗**；"GM 后正文前只能写作指令"的细分同样只留框架。
- 主循环拆为 stepPlayer/stepCharacter/stepGm/stepProse + continuePipeline（按 phase 续跑）；归档写入时机不变（下一步启动时归档上一步）。

---

## 10.3 M2 设计定稿（2026-07，grill-with-docs 会话冻结）

M2 机制经 13 轮设计问答冻结。词汇与冲突消解记录见 `CONTEXT.md`；架构决策记录见 `docs/adr/0001`（计时器 = 唯一时间跨度设置单元，耗时废除）、`docs/adr/0002`（事件感知 = `known_by` tags 唯一通道）。正文 §1.1/§1.2/§2/§5/§6 已按定稿改写，本节记录不随正文走的要点。

**GM 裁决包契约（M2）**——字段从简（节约 token）：

```json
{
  "events": [{"text": "@C1002 对 @C0 说……", "tags": ["known_by:C0", "known_by:C1002"], "location": "灯塔顶层"}],
  "narrativity": "full",
  "deltas": [{"path": "…", "op": "…", "value": "…"}],
  "timer": [{"cid": "C1002", "span": {"h": 1, "min": 30}}],
  "location": [{"cid": "C1002", "location": "灯塔顶层"}]
}
```

- **事件数 = GM 计划的新组划分**：普通周期 = 1 个；组分裂/重组时 = 各新组（单人组/多人组）各一个事件。逐事件 `tags`（`known_by` 必填，缺省 = 本轮参与者）；跨场景对话事件 `location` 置空。
- **输出顺序：先写事件、后定 timer/location**——叙事推理自然约束变量分配（组划分表面自动、实际由 GM 决定）。
- `narrativity` 包级；`timer` 为相对偏移 span（§1.2），**GM 契约：必输出非 0 timer**（§5.1）。普通轮中 `timer` 的 CID 集合必须与本轮行动者精确一致且不重复，`location` 只能是不重复的行动者子集；该上下文级契约与 schema parse 一起在 GM 的一次重试循环内校验，失败包不得应用。GM 裁决后存在无 timer 的角色 → 警告日志（防漏设沉底）。
- **事件写作纪律是提示词协议层约束，不做程序强校验**（叙事自由度优先——参与者不知道的事，known_by 就不标他们）：事件人物一律 @CID 占位不写真名、known_by 谁实际感知标谁、空数组 = 程序补全本轮全部行动者。**摘要制**（提示词层把事件称"摘要"以约束 GM 输出，领域术语仍是事件）：不完整转述角色台词，对对话内容与情节做摘要和意义性总结；绝不使用人称代词（演员表角色一律 @CID）；不颠倒言行顺序、只记录已发生的（不管对话是否完成、反应是否做出）；多条目时记录事实而非视角、条目间禁止任何重合；先写事件、后定 timer/location。timer 分组纪律：集体行为/有交互的角色给相同 span，各忙各的/准备离开的给不同 timer 或 location。提示词模板必须逐字段说明含义，**示例禁用真实 CID**（用 `<CID_甲>` 抽象占位），防止模型照抄示例造成感染。演员表显示名以 characters 档内副本的 `name` 为唯一真相（C0 与 NPC 同规，玩家改名即生效）。

**时间轴与轮状态全派生，无独立存储**：调度 = 扫描角色 timer 取最小；组 ← location+timer / 邀请并入；行动顺序 ← initiative 变量（{先攻值， 组编号}）；游标与 phase ← archive 的 seq 序列推断——archive 不存 pipeline 快照，回溯零特例。timer 变更走 var_changes 通道（`{cid}.timer`），回溯天然可逆。

**simulator.ts 重写为派生纯函数集**：`next_due(characters)` / `derive_groups(characters)` / `visible_events(events, cid)`；Event 堆调度与 location 感知过滤废除。"纯逻辑、禁 IO、元测试守护"纪律不变（test/simulator.test.ts 脚本化喂入模式保留）。

**正文渲染**：一次 GM 包至多一次正文调用（narrativity 包级，skip 则无）；渲染范围 = 包内全部事件（蒙太奇叙事，不做玩家视角过滤；限定视角未来经 known_by 过滤扩展）。GM 事件经字段提取、提示词组装为"何时何地，哪些人做了什么"注入。**指称占位符契约**：正文存带 `[[称呼|@CID]]` 的原文（直接引语内豁免），渲染按读者分视图——玩家显示 → 称呼；角色注入 → 按读者 relations（name → impression → 保留 @CID）；GM 注入 → 称呼（@CID）；正文 agent 的上一轮正文注入保持原文（格式学习通道）。合规是提示词协议层约束，正则不匹配即原样保留（docs/adr/0003）。

**`time.json`**（世界设定集新文件）：`{start: {y,m,d,h,min}, periods: [{key, from, to}]}`——start = clock 0 的世界时刻锚点；periods = 24h 时段区间表。渲染 = "第N日·{时段}"。随设定集拷入存档（档内副本原则，同 lore）。

**白滩镇 4 角色开局**（含主控）：timer = {C0: 0, NPC1: 0, NPC2: 0, NPC3: 1h}；location = {C0 = NPC1, NPC2 = NPC3}——开局即覆盖多人在场组（C0+NPC1）/ 单人组（NPC2）/ 错时单人组（NPC3）全部情形。reaction 开场全员同值；初始人际关系由用户真人编写（相互关系随人数快速膨胀，LLM 写入易漏）。

**Group 变量编码**（M2-b 起，inScene/inTalk 双变量与 4 组合状态表已废除）：`group = {组编号, 组位置}`——`组编号` 恒有值（0 = 单人组，[1,n) = 多人组编号；同组判定 = 组编号相同且 ≠ 0）；`组位置` 总绑定先攻最高者的 CID（= 其 location，领头者变更重绑，同值取最小 CID）。入组位置 ≠ 组位置 → 先攻 -1。组 id 指派并保稳（精确匹配保留；纯减员/纯增员继承原 id，分裂取最大子集、并列取最小 CID；合组人少并入人多），**不得由 hash(location+timer) 派生**（连续轮每圈重设 timer 会换 id，连续场景判定即垮）。调度判定统一为 timer 模式（扫描最近到期者），无对话模式特例；单活跃组不变量与同刻多组串行规则见 §1.1。

**突发鉴定（surge check）**：每次 GM 裁决结束后执行的三步机械鉴定（DESIGN §5.5 引信制的细化），可打断 timer——长期行动（闭关、赶路）不再必然跑满。记 Δt = 当前时刻到最近 timer 的时间差，g = 差值函数（地点 level − 角色 level）∈ (-50,50)：

1. **是否突发**：触发概率 = (d20-1) + f(Δt)，f ∈ (0,80] 且随 Δt 单调递增 → (0,99]%；
2. **良性/恶性**：良性概率 = (g + 50)%（差值为 0 时对半）；
3. **程度**：程度 = (d20-1) + |g| + 30 ∈ [30,99)（+30 = 避免无意义事件的修正值），为烈度百分比，供内容生成参考。

**所有 [a,b] 区间值均为百分比，直接按百分比概率检定**（①②掷 d100 ≤ 点数即成立）。由此 **location 结构化为 `{name, level}`**（name 仍是 GM 自由文本；同地判定按 name），**level 是角色必备变量**。鉴定函数在程序侧实现（专门函数，确定性）；突发事件的解释性提示词（内容生成引导）未来再做，此处先留记录。

---

## 10.4 M2-b 设计定稿（2026-07，grill-with-docs 第二轮会话冻结）

M2-b 机制经设计问答冻结。词汇以 `CONTEXT.md` 为准（无判定轮/行动周期/标记/邀请/Group 变量均已立词条，含两次修订记录）；架构决策记录见 `docs/adr/0004`（GM 按需激活 + 同步组单变量化）；正文 §1.1/§1.2/§5/§6/§9 已按定稿改写，本节只记决策落点与悬置项。

**核心转向**：对话组/对话轮概念整体废除——说话是普通行动，不需要专门发起机制；无判定是轮的默认形态，GM 激活是例外（标记立即触发 / 硬保险 N 周期末触发）。动机：擦桌子、抬头看一眼类行为不该占用 GM 判定；对话轮"只许说话"损失场景表现力。

**已冻结决策**（细节见正文各节）：

1. **行动周期** = 连续场景内所有成员各行动一次；硬保险 N 存 config.json（设置页可编辑）；**周期计数 X = 世界变量落盘**（完成 +1、GM 清零，见第 3 条）；玩家输入计入周期计数。
2. **GM 标记触发立即激活**（同先攻批全员行动完才激活），硬保险周期末激活；中途 GM 必须设 timer 且必为非 0。
3. **行动顺序表**（取代 SEQ 差规则）：顺序派生（initiative 现排）；**已行动位 = 角色变量**（快照落盘 var_changes，行动置位、周期完成/后台重置清零）；**周期计数 X = 世界变量**（完成 +1、GM 清零、达 N 周期末 GM）；周期补完（前台不变 ⇒ 状态不变，中途 GM 无特例）。GM/正文步不参与调度语义。
4. **五标记**走 DecisionPackage 结构化 `markers` 数组（gm_request/leave 互斥/recall/contact/confirm），程序只读结构化字段；标记不进工作集、不进任何注入，只有即时程序作用。
5. **离开标记** = 程序化归 0 + 超大 timer，下次 GM 统一结算；**所有角色（含玩家）统一程序化**（"玩家离场立 GM 请求"例外已废除），离场者过去言行继续注入原组直到清算，后续角色经 `{{departure_notices}}` 占位符获知。**召回**跨周期有效（未结算 = 离开→GM 结算前），复用 {先攻值，组编号}。
6. **inScene/inTalk 合并为 Group = {组编号， 组位置}**：组位置绑定先攻最高者 CID；入组位置 ≠ 组位置 → 先攻 -1；先攻变量结构化为 {先攻值， 组编号}（组编号变化即重置，归 0 除外）。
7. **单活跃组不变量**；同刻多组串行（组内最高先攻降序，同值比 CID）；后台判定纯按 timer。
8. **邀请流程（延迟生效）**：目标至少含一个异地角色，本地默认同意、异地常规顺序应答；联系标记**触发 GM 立即结算**（双方看不到标记），邀请者组下次回前台才激活异地受邀者；拒绝 → timer 还原 + 失去频道（全同地则频道自动清除）；接受 → 归 0 待 GM 重设、并入组（位置不同 -1），首轮回复计入本周期已行动。**频道变量**：邀请发起即分配给各方、拒绝者失去、退组清除；防重入（不进联系人列表）+ 生命周期（全同地全清、非组位置者离场等结算）。
9. **AV 标签位方案撤回**：感知过滤不在组层做，M2-b 组内注入全透传；私密性归 P2 顶层 TAG 过滤。
10. **玩家输入格式统一**（三块：台词/行动/内心（含意图）+ 标记按钮，组装为 NPC 同构 DecisionPackage）列入 M2-b 必做；契约同步简化：inner 必填（intent 并入），action/dialogue 至少其一。暂停选项（自动继续/每轮/GM 前/GM 后/正文后，前二互斥、后三自由组合）。
11. **周期中途增减**：插入仅两种形态（邀请逐个入组 / timer 对齐合组先补完周期再人少并入人多重排序）；中途退出轮替下一未行动者，全部已行动 → 常规触发判定。

**悬置项**：

- 标记经过（离开/召回/邀请）的**注入渲染**：推迟到提示词组装设计阶段，与 P2 TAG 过滤一并设计；M2-b 落地时无渲染。
- 退出期望管理（防止无判定轮无止境闲聊）：提示词设计（静态文案/周期计数强化/对话预期变量），后置。
- 远程成员"注入标注"的具体格式：随提示词组装设计一并定。
